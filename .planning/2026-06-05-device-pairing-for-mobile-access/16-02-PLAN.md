---
phase: 16-device-pairing-for-mobile-access
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - devices.js
autonomous: true
requirements: [AC4, AC5, AC6, AC8]
must_haves:
  truths:
    - "tests/devices-json.test.js (Wave 0) goes GREEN"
    - "Relevant cases of tests/device-revoke-rebuild.test.js (Wave 0) also green (the devices-API half of the cycle)"
    - "Raw token NEVER appears in devices.json on disk — only sha256:<64hex> token_hash (AC8)"
    - "findByToken uses crypto.timingSafeEqual for constant-time hash compare (AC8 defence-in-depth)"
    - "Persistence uses plain writeFileSync — Option A per planner pin, matches sessions.js:762 and config.js:218 precedent; no novel atomic-rename pattern introduced in Phase 16"
    - "Module loads at server boot before the wss is wired (server.js will be edited in 16-04/16-05 to require + load it)"
  artifacts:
    - path: devices.js
      provides: "Persistent device store with load/save/list/findByToken/add/remove/touchLastSeen + token mint/hash helpers"
      contains: "module.exports = { load, save, list, isEmpty, findByToken, add, remove, touchLastSeen, mintToken, hashToken, DEVICES_PATH, BOOTSTRAP_PATH, clearBootstrap }"
  key_links:
    - from: devices.js
      to: paths.js
      via: "DATA_DIR import"
      pattern: "require\\('./paths'\\)"
    - from: devices.js
      to: server.js (Wave 2 16-05 will require and devices.load() at boot)
      via: "require('./devices')"
      pattern: "devices.load\\(\\)"
    - from: devices.js
      to: tests/devices-json.test.js
      via: "freshDevices() require-cache wipe + require('../devices.js')"
      pattern: "test contract from Wave 0"
---

<objective>
Wave 1, Plan 1 of 3 — build the `devices.js` persistence layer. Pure data + crypto module:
load/save the JSON store at `${DATA_DIR}/devices.json`, mint opaque tokens + device IDs, hash
tokens before persistence, and look up devices by raw token using constant-time hash compare.
No HTTP, no WebSocket — those are in 16-04 and 16-05. This module exists to turn the Wave 0
`tests/devices-json.test.js` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-05-device-pairing-for-mobile-access/SPEC.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-CONTEXT.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-01-PLAN.md

# Pattern analogs
@sessions.js
@config.js
@paths.js
@plugin-loader.js

# The contract this plan satisfies
@tests/devices-json.test.js
</context>

<interfaces>
<!-- Verified-against-HEAD facts the executor needs in the prompt itself,
     so they don't have to spelunk to find them. -->

## paths.js DATA_DIR (paths.js:5)
```
const DATA_DIR = process.env.CLIDECK_DATA_DIR || join(os.homedir(), '.clideck');
```
Tests rely on the CLIDECK_DATA_DIR override (PATTERNS §4.2). The devices module MUST
derive its paths from `require('./paths').DATA_DIR` — NOT from `os.homedir()` directly.

## Existing persistence pattern — plain writeFileSync (NOT atomic)
- sessions.js:762 — `writeFileSync(SAVED_PATH, JSON.stringify(data, null, 2));`
- config.js:218   — `writeFileSync(CONFIG_PATH, JSON.stringify(config, null, 2));`

RESEARCH §3 verified neither uses atomic-rename. CONTEXT.md's claim that sessions.json
follows atomic-rename was incorrect. PATTERNS §2.2 explicitly recommends Option A
(plain writeFileSync) for Phase 16 to match the existing project discipline. **PLANNER
PIN: Option A. Document the choice in the SUMMARY.** If Lance wants atomic-rename later,
do it as a Phase 17 retrofit covering sessions.json + config.json + devices.json together
so the project doesn't have one atomic file and two non-atomic siblings (the discipline-
asymmetry footgun called out in PATTERNS §2.2).

## Schema (CONTEXT.md "scope" section — locked)
```json
{
  "version": 1,
  "devices": [
    {
      "id":          "dev_<22 base64url chars>",
      "label":       "Lance iPhone",
      "fingerprint": "ua-hash-<sha256 first 12 chars of UA at pair-time>",
      "paired_at":   "2026-06-05T12:34:56.789Z",
      "last_seen":   "2026-06-05T12:34:56.789Z",
      "token_hash":  "sha256:<64 hex chars>"
    }
  ]
}
```

## Existing crypto usage idioms (PATTERNS §1 row 10, RESEARCH §C)
- `sessions.js:4` — `const crypto = require('crypto');`
- `runtime.js:32`, `config.js:191`, `paste-blobs.js:106` — `crypto.randomUUID()`, `crypto.randomBytes`
- `plugin-loader.js:2, 34` — `crypto.createHash('sha256').update(...).digest('hex')` precedent for the sha256: prefix

## Module shape from RESEARCH §10.2 (canonical reference)
The executor MUST mirror the structure in @.planning/.../16-RESEARCH.md §10.2 verbatim
for `load`, `save`, `hashToken`, `safeEqualHash`, `findByToken`, `mintToken`, `mintDeviceId`,
`add`, `remove`, `touchLastSeen`, `list`, `isEmpty`, `clearBootstrap`. The full exports
list is:
```
module.exports = {
  load, save, list, isEmpty,
  findByToken, add, remove, touchLastSeen,
  mintToken, hashToken,
  DEVICES_PATH, BOOTSTRAP_PATH, clearBootstrap,
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create devices.js with load/save/list/isEmpty + token hashing helpers</name>
  <files>devices.js</files>
  <read_first>
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.2 — canonical devices.js shape (load, save, hashToken, safeEqualHash, findByToken, mintToken, mintDeviceId, add, remove, touchLastSeen, list, isEmpty, clearBootstrap). The executor SHOULD treat this as the implementation reference and reproduce it; minor adaptations are fine (e.g. console.log wording) but the exported function signatures are fixed by the Wave 0 test.
    - @sessions.js lines 1-10, 705-784 — `loadSessions()` / `saveSessions()` style and `console.log(\`Loaded N session(s)\`)` wording precedent.
    - @config.js lines 1-30, 200-220 — `loadConfig()` / `save()` style for the parse-or-fallback pattern.
    - @paths.js lines 1-45 — DATA_DIR export and the `mkdirSync(DATA_DIR, { recursive: true })` boot-time guarantee.
    - @plugin-loader.js line 2, 34 — sha256 hex precedent.
    - @tests/devices-json.test.js (Wave 0) — the contract to satisfy. Re-read it.
    - Verify the line numbers against the current HEAD; PATTERNS.md was pinned to commit `b4a09fb` and Phase 15's eventual merge may shift them. Re-grep `function loadSessions|function save\\b|writeFileSync` in sessions.js + config.js + paths.js before quoting.
  </read_first>
  <behavior>
    After this task lands, importing `require('./devices.js')` exposes:
    - `DEVICES_PATH` (string, `${DATA_DIR}/devices.json`)
    - `BOOTSTRAP_PATH` (string, `${DATA_DIR}/bootstrap.otp`)
    - `load()` — reads DEVICES_PATH, parses JSON, populates a module-scope `store = { version, devices: [] }`. Missing file → `{ version: 1, devices: [] }`. Parse error → log a warn and reset to empty (don't throw).
    - `save()` — `writeFileSync(DEVICES_PATH, JSON.stringify(store, null, 2))` per Option A pin. No tmp file, no rename.
    - `list()` — returns a slice (defensive copy) of `store.devices`.
    - `isEmpty()` — `store.devices.length === 0`.
    - `hashToken(rawToken)` — returns `'sha256:' + createHash('sha256').update(raw).digest('hex')`. Length 71.
    - `safeEqualHash(a, b)` — length-checked `crypto.timingSafeEqual(Buffer.from(a), Buffer.from(b))`; returns false (not throws) on mismatched length / non-string input.
    - `mintToken()` — `crypto.randomBytes(32).toString('base64url')`. 43-char output.
    - `mintDeviceId()` — `'dev_' + crypto.randomBytes(16).toString('base64url')`. 26-char output.
    - `findByToken(rawToken)` — null on falsy input; otherwise hash + O(n) `safeEqualHash` loop; returns the matching device record or null.

    The bootstrap helpers (`clearBootstrap`) belong here per RESEARCH §10.2; the bootstrap *minting* lives in pair-otp.js (Plan 16-03). `clearBootstrap()` = `try { unlinkSync(BOOTSTRAP_PATH); } catch {}`.

    Concurrency: load() runs once at boot. save() runs on add/remove/touchLastSeen. There is no debouncer — RESEARCH §10.2 flags that touchLastSeen on every WS reconnect would write hundreds of times per session; the planner accepts this for Phase 16 (the workload is "Lance + ~3 phones" so even 100 writes/day is fine) and documents the debounce as a Phase 17 follow-up. Note this trade-off in the SUMMARY.
  </behavior>
  <action>
    Create `/home/clideck/projects/clideck/devices.js`. Implement per RESEARCH §10.2. Concrete prose, no inline code blocks in this <action> per planner contract — the implementation reference IS RESEARCH §10.2 and the executor reproduces that shape.

    Module-scope state: `let store = { version: 1, devices: [] };` — this is intentional mutable module-scope state matching the sessions.js / config.js precedent (PATTERNS §1 row 10). Tests use the require-cache wipe to get a fresh closure.

    For `load()`: log `[devices] Loaded N paired device(s)` on success, mirror the sessions.js wording. On parse error, log `[devices] Failed to parse {path}: {message}. Starting empty.` Do not throw.

    For `hashToken`: import `createHash` from `'crypto'` (or `require('crypto').createHash`); use the existing 'sha256:' string-prefix idiom from plugin-loader.js for the lookup-table-shaped hash.

    For `safeEqualHash`: guard with `if (typeof a !== 'string' || typeof b !== 'string') return false; if (a.length !== b.length) return false;` BEFORE `crypto.timingSafeEqual`, because timingSafeEqual throws on length mismatch (RESEARCH §2 R-2 table).

    For `add({ label, uaFingerprint, rawToken })`: mint a deviceId, slice the label to 32 chars, trim it, fall back to 'Device' if empty (per RESEARCH §10.2), record paired_at and last_seen as `new Date().toISOString()`, store `token_hash: hashToken(rawToken)`, push, save, return the record.

    For `remove(deviceId)`: filter the array, save only if length changed, return the number removed (0 or 1).

    For `touchLastSeen(deviceId)`: mutate the matching record's `last_seen` to now and save. If no match, no-op. Document the no-debouncer choice inline.

    Export the full surface per RESEARCH §10.2.

    **Do NOT modify server.js in this task** — wiring is in 16-04 and 16-05. This plan is the data module only, so it can be developed and tested in pure isolation against the Wave 0 spec.
  </action>
  <verify>
    <automated>npx vitest run tests/devices-json.test.js --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f devices.js`
    - Exports the documented surface: `node -e "const d = require('./devices'); console.log(Object.keys(d).sort().join(','))"` produces something containing: `BOOTSTRAP_PATH,DEVICES_PATH,add,clearBootstrap,findByToken,hashToken,isEmpty,list,load,mintToken,remove,save,touchLastSeen`
    - hashToken produces the expected shape: `node -e "console.log(require('./devices').hashToken('x'))"` outputs `sha256:` + 64 hex chars
    - mintToken produces 43 chars: `node -e "console.log(require('./devices').mintToken().length)"` outputs `43`
    - mintDeviceId is internal (per RESEARCH §10.2 exports list it's NOT exported; `add()` calls it internally). Verify: `node -e "console.log(typeof require('./devices').mintDeviceId)"` outputs `undefined`
    - Wave 0 contract passes: `npx vitest run tests/devices-json.test.js` exits 0
    - No new .tmp file pattern (Option A pin): `grep -F '.tmp' devices.js` returns no output
    - timingSafeEqual is used for hash compare: `grep -F 'timingSafeEqual' devices.js`
  </acceptance_criteria>
  <done>devices.js exists, exports the documented surface, turns tests/devices-json.test.js GREEN. No production code beyond this module touched.</done>
</task>

</tasks>

<verification>
The Wave 0 spec that contracts this plan must pass after this task lands:

```bash
npx vitest run tests/devices-json.test.js --reporter=verbose
# Expected: exit 0, all 9 it-blocks pass.
```

The other tests authored in 16-01 that touch devices.js (tests/device-revoke-rebuild.test.js,
tests/pair-redeem.test.js, tests/ws-auth-gate.test.js, tests/revoke-closes-socket.test.js,
tests/bootstrap-otp.test.js) will still fail because they depend on pair-otp.js / sessions.js
edits / routes / etc. that this plan does not deliver. That is expected — 16-03 onwards
close the remaining gaps.

Smoke regression check: existing suite untouched.

```bash
npx vitest run --reporter=verbose 2>&1 | tail -10
# Expected: pre-existing test counts unchanged + 9 new green from tests/devices-json.test.js.
```

Per CLAUDE.md §1 verify the round-trip lives on disk in the intended shape:

```bash
node -e "
  process.env.CLIDECK_DATA_DIR = require('os').tmpdir() + '/clideck-smoke-' + Date.now();
  require('fs').mkdirSync(process.env.CLIDECK_DATA_DIR, { recursive: true });
  const d = require('./devices');
  d.load();
  const rec = d.add({ label: 'Smoke', uaFingerprint: 'ua-x', rawToken: 'smoke-token' });
  const content = require('fs').readFileSync(d.DEVICES_PATH, 'utf8');
  console.log('content includes raw token?', content.includes('smoke-token'));   // expect: false
  console.log('content includes sha256:?',    content.includes('sha256:'));      // expect: true
  console.log('deviceId starts dev_?',         rec.id.startsWith('dev_'));        // expect: true
"
```

The first console.log MUST print `false` per AC8 (token never in persistence).
</verification>

<success_criteria>
- [ ] devices.js exists at the repo root (alongside sessions.js, config.js, handlers.js)
- [ ] `tests/devices-json.test.js` passes (Wave 0 contract → GREEN)
- [ ] No raw token leaks into devices.json on disk (AC8 smoke-checked above)
- [ ] Existing vitest suite still green (no regressions)
- [ ] Plain writeFileSync used (no atomic-rename pattern introduced — Option A pin)
- [ ] crypto.timingSafeEqual is used in the lookup path
- [ ] No edits to any other production file (this plan is the data module in isolation)
- [ ] Commit lands on `main`, NOT pushed (CLAUDE.md §3)
- [ ] 16-02-SUMMARY.md written documenting: the Option A pin and its rationale (PATTERNS §2.2),
       the no-debounce choice for touchLastSeen and the Phase 17 follow-up it implies,
       the Wave 0 spec result before/after.
</success_criteria>

<output>
Create `.planning/2026-06-05-device-pairing-for-mobile-access/16-02-SUMMARY.md` with:

- The Option A choice (plain writeFileSync) called out at the top with its 2-line rationale: matches
  sessions.js:762 and config.js:218 precedent; avoids introducing a single atomic-rename file that
  would create a discipline asymmetry across the project. Phase 17 follow-up TODO: retrofit all three
  persisters with the same atomic-rename helper in one go.
- Confirmation that `tests/devices-json.test.js` went GREEN.
- The remaining RED tests from Wave 0 that this plan deliberately leaves untouched (and which plan
  closes each one).
- A note about touchLastSeen having no debouncer (acceptable at Lance's 1-user / ~3-device scale;
  Phase 17 candidate for batching via the same `sessions.startAutoSave` interval idiom).
- Quote the verification commands and their actual output (CLAUDE.md §1 verify-before-claiming-done).
</output>
