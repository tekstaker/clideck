---
phase: 16-device-pairing-for-mobile-access
plan: 08
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03, 16-04, 16-05, 16-06, 16-07]
files_modified:
  - .planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md
autonomous: false
requirements: [AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8, AC9]
must_haves:
  truths:
    - "Full vitest suite runs and the 7 new Phase 16 specs (pair-otp, devices-json, pair-redeem, ws-auth-gate, revoke-closes-socket, bootstrap-otp, device-revoke-rebuild) are GREEN"
    - "Full Playwright suite is attempted; pair-flow + revoke-flow results captured (Chromium libs availability documented; if libs missing, the WSL-Chromium-libs-deferred precedent from Phase 15 applies and the manual R3 path is the canonical gate)"
    - "9-AC matrix in 16-VERIFICATION.md maps each AC to PASS automated / PASS via existing surface / DEFERRED to manual, with file:line evidence for each"
    - "R3-precedent device smoke (Lance's actual phone over the VPN) documented as the canonical real-mobile gate per Phase 15's D-14 precedent; checkpoint pauses for Lance to run it"
    - "Token-hygiene audit (AC8 deep-dive): grep across the diff and the on-disk DATADIR proves no raw-token leakage to logs / persistence"
  artifacts:
    - path: .planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md
      provides: "Canonical verification document modelled on 15-VERIFICATION.md; 9-AC matrix + suite results + token-hygiene audit + Lance's manual-gate checklist"
      contains: "## Acceptance Criteria"
  key_links:
    - from: 16-VERIFICATION.md
      to: All 7 Phase 16 PLAN.md files + 16-SUMMARY.md per plan
      via: "cross-reference table mapping AC → plan → evidence"
      pattern: "16-0[1-7]-SUMMARY.md"
---

<objective>
Wave 3, Plan 2 of 2 — the verification rollup. Runs the full vitest + Playwright suites, captures
the results, audits AC8 token-hygiene with grep across the diff and the live on-disk DATADIR,
writes 16-VERIFICATION.md mapping each AC to its automated / manual / deferred evidence, and
pauses for Lance to run the R3-precedent real-mobile smoke over the VPN. Final plan of Phase 16.

This plan is `autonomous: false` because Task 4 is a checkpoint:human-verify pause for Lance to
run the real-device smoke. Tasks 1-3 are fully automated; Task 4 is the canonical real-mobile
gate per Phase 15's D-14 precedent. Task 5 (write 16-VERIFICATION.md) is post-checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-05-device-pairing-for-mobile-access/SPEC.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-CONTEXT.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md

# All prior plans + their summaries
@.planning/2026-06-05-device-pairing-for-mobile-access/16-01-PLAN.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-02-PLAN.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-03-PLAN.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-04-PLAN.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-05-PLAN.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-06-PLAN.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-07-PLAN.md

# Verification template — Phase 15's matches Phase 16's shape
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md
</context>

<interfaces>
<!-- Reference shape for 16-VERIFICATION.md. Phase 15's 15-VERIFICATION.md is the template;
     Phase 16's is structurally identical with 9 ACs instead of 8. -->

## 15-VERIFICATION.md sections to mirror
- Header (Phase / branch / final commit / verified-by)
- TL;DR (one paragraph summary of automated pass/fail counts + flake notes)
- Acceptance Criteria matrix (table: # | SPEC AC | Status | Evidence)
- Token-hygiene audit (Phase 16 specific — AC8 deep-dive — new section vs. Phase 15)
- Vitest results (file count / pass count / fail breakdown)
- Playwright results (suite-level pass count + per-spec breakdown + Chromium-libs availability note)
- Real-device R3 path (Lance's eye verification checklist)
- Follow-ups / Phase 17 candidates

## Status taxonomy (mirror Phase 15)
- ✅ AUTOMATED — passing in vitest or Playwright
- ✅ AUTOMATED (server contract) / ⚠ E2E ASSERTS CLIENT-SIDE — server proven, e2e flake from infra not contract
- ✅ VERIFIED VIA EXISTING SURFACE — proven by an existing test or grep
- ⚠ FLAKE — passes intermittently; root cause and mitigation noted
- 🔵 DEFERRED MANUAL — real-device path, checkpoint:human-verify
- ❌ FAILING — actual contract regression; PHASE 16 BLOCKER (must not exist when this plan commits)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full vitest suite + capture per-file pass/fail counts</name>
  <files>(no edits — capture only)</files>
  <read_first>
    - @.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md — section format reference.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-01-PLAN.md — the 7 specs authored.
  </read_first>
  <behavior>
    Run `npx vitest run --reporter=verbose 2>&amp;1 | tee /tmp/16-vitest-final.log` from the project root. Capture:
    - Total test files run
    - Total it-blocks passed/failed/skipped
    - Per-Phase-16-spec pass count (pair-otp, devices-json, pair-redeem, ws-auth-gate, revoke-closes-socket, bootstrap-otp, device-revoke-rebuild)
    - Any failures: capture the file + assertion + line

    Expected outcome per Wave 0-3 contracts: ALL 7 Phase 16 specs GREEN, no regressions in the existing suite.
  </behavior>
  <action>
    1. From the project root run: `npx vitest run --reporter=verbose 2>&amp;1 | tee /tmp/16-vitest-final.log`
    2. Extract the summary line (e.g. `Tests: N passed, 0 failed (X)`) and any failure detail.
    3. Compute the Phase 16 contribution: `cat /tmp/16-vitest-final.log | grep -E "tests/(pair-otp|devices-json|pair-redeem|ws-auth-gate|revoke-closes-socket|bootstrap-otp|device-revoke-rebuild)\.test\.js" | tee /tmp/16-vitest-phase16-only.log`
    4. Store the captured output for Task 5's writeup. Do NOT paste secrets — the test files use synthetic test tokens (`test-token-NOT-real-...`) so this is fine.

    IF any Phase 16 spec FAILS: this is a Phase 16 BLOCKER. Do not proceed to checkpoint until the originating plan (16-02 .. 16-07) is re-run and re-verified. Per CLAUDE.md §1, do not paper over with "should work".
  </action>
  <verify>
    <automated>test -f /tmp/16-vitest-final.log &amp;&amp; tail -20 /tmp/16-vitest-final.log &amp;&amp; grep -c "FAIL\|✗" /tmp/16-vitest-final.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/16-vitest-final.log exists and contains a "Tests:" summary line
    - All 7 Phase 16 specs visible in the run output
    - Pre-existing tests (check-cwd, resumable, session-pause, ws-send-guard, etc.) still GREEN — no regressions from the auth-gate splice into server.js / sessions.js / handlers.js
    - Zero unexplained failures (any flake noted with its root cause)
  </acceptance_criteria>
  <done>Full vitest run complete; results captured; any failures triaged with explicit blocker / flake / unrelated classification.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full Playwright suite + capture per-spec results (defer Chromium-libs failure gracefully)</name>
  <files>(no edits — capture only)</files>
  <read_first>
    - @.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md TL;DR — Phase 15's Playwright run experience (Chromium libs were available; 37/44 passed; the 7 failures were triaged into 2 unrelated / 2 Phase-12 flakes / 3 real gaps that fed into 15-UI-REVIEW).
    - @playwright.config.js — current playwright config (HOME tempdir, port 4099, single project chromium).
    - @e2e/pair-flow.spec.js (16-01) — Wave 0 e2e for AC1+AC2+AC3+AC7.
    - @e2e/revoke-flow.spec.js (16-01) — Wave 0 e2e for AC5 + D-06.
  </read_first>
  <behavior>
    Run `npx playwright test --reporter=list 2>&amp;1 | tee /tmp/16-playwright-final.log`. Capture:
    - Per-spec pass/fail
    - Phase 16's two new specs (pair-flow, revoke-flow) specifically
    - Any pre-existing flakes (per Phase 15's precedent: ctrl-v-paste, session-indicator-mutex idle→working etc.)

    Handle the "Chromium libs missing" failure mode gracefully per Phase 15's D-14 precedent:
    - If Playwright fails to launch Chromium with an "executable doesn't exist" / "missing libraries" error, capture the exact error message and DEFER pair-flow / revoke-flow to the real-device R3 manual gate (Task 4). Do NOT block the phase on a WSL Chromium-libs issue; document it instead.
    - If Chromium launches but pair-flow / revoke-flow fail with a real assertion error, this is a Phase 16 BLOCKER. Do not proceed.
  </behavior>
  <action>
    1. From the project root: `npx playwright test --reporter=list 2>&amp;1 | tee /tmp/16-playwright-final.log`
    2. If exit code != 0, inspect the log:
       - "executable doesn't exist" / "browserType.launch: Host system is missing dependencies" → CHROMIUM-LIBS-MISSING. Document and defer pair-flow / revoke-flow to Task 4 (manual real-device).
       - Real assertion failures on Phase 16 specs → BLOCKER. Stop and re-open the originating plan.
       - Real assertion failures on pre-existing specs → triage as Phase 15 precedent: 2-cat split (unrelated pre-existing flake vs. Phase 16 regression). Pre-existing flakes pass through; regressions are BLOCKERS.
    3. Extract the per-spec pass/fail block for the writeup.
  </action>
  <verify>
    <automated>test -f /tmp/16-playwright-final.log &amp;&amp; tail -30 /tmp/16-playwright-final.log &amp;&amp; grep -cE "pair-flow|revoke-flow" /tmp/16-playwright-final.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/16-playwright-final.log exists
    - If Chromium runs: pair-flow.spec.js GREEN AND revoke-flow.spec.js GREEN (Phase 16 e2e contracts satisfied)
    - If Chromium-libs missing: the log captures the exact error message AND Task 4's manual checklist explicitly covers AC1, AC2, AC3, AC5, AC7 to compensate
    - No silent skips — every Phase 16 spec accounted for (pass / fail / deferred-with-reason)
  </acceptance_criteria>
  <done>Playwright run complete OR deferred-with-documented-reason; Phase 16 e2e specs accounted for; pre-existing flakes triaged.</done>
</task>

<task type="auto">
  <name>Task 3: AC8 token-hygiene audit — grep diff + audit live DATADIR</name>
  <files>(no edits — audit only)</files>
  <read_first>
    - @.planning/2026-06-05-device-pairing-for-mobile-access/SPEC.md AC8 — "No log line (server stdout, request log, client console) contains the raw token after /pair/redeem returns it. Only token_hash appears in persistence and logs."
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-CONTEXT.md AC8 + the D-02 bootstrap-banner exception.
    - All Phase 16 source files: devices.js, pair-otp.js, auth-gate.js, routes/pair.js, server.js (Phase 16 splices only), handlers.js (Phase 16 arms only), sessions.js (closeDevice only), public/pair.html, public/js/pair.js, public/js/app.js (Phase 16 splices only), public/js/settings.js (renderLinkedDevices only).
  </read_first>
  <behavior>
    AC8 has two surfaces: code (no console.log of raw token) and persistence (no raw token in devices.json / bootstrap.otp / any other on-disk artifact).

    Code-side grep audit:
    1. `git diff main~7..HEAD -- '*.js' 'public/**/*.html' 'public/**/*.js' | grep -E "console\\.(log|error|warn|info|debug)" > /tmp/16-ac8-console.log` — collect every console call introduced by Phase 16.
    2. Review each: only `console.log` and `console.warn` of synthetic strings / device IDs / non-sensitive metadata are acceptable. The bootstrap banner in pair-otp.js is the documented intentional exception (D-02 / CLAUDE.md §13 carved-out scope). NO console call should include the raw token from /pair/redeem.

    Persistence-side audit:
    1. Boot the server with a fresh DATADIR.
    2. Bootstrap-pair via /pair/redeem; capture the returned `token` value into a local variable (do NOT paste into the SUMMARY).
    3. `grep -F "$TOKEN" "$DATADIR/devices.json"` → must return NO MATCH (token_hash is sha256:... only).
    4. `grep -F "$TOKEN" "$DATADIR"/* 2>/dev/null` → must return NO MATCH across any file under DATADIR (sessions.json, config.json, bootstrap.otp post-redeem-deletion).
    5. Confirm bootstrap.otp was deleted on first successful redeem.
    6. Mint a second OTP via /pair/mint-otp + the X-Clideck-Device-Token header. Pair a second device. Confirm both tokens are absent from disk and only their hashes are present.

    Log-side audit (during the boot):
    1. Capture the server's stdout into /tmp/16-ac8-stdout.log during the bootstrap + pair flow.
    2. Confirm the only OTP appearance is the bootstrap banner (D-02 exception); the user-minted OTP from step 6 should NOT appear in stdout.
    3. Confirm NO raw token appears in stdout at any point.
  </behavior>
  <action>
    1. Run the grep audits above; capture output to /tmp/16-ac8-*.log files.
    2. Run the persistence audit script (sourced from the recipe in 16-04-PLAN's verification block + extended for the mint-otp + second device). DO NOT paste token values into commit/SUMMARY; reference by `<43-char base64url>`.
    3. Run the log audit during the boot.

    If ANY of the audits surface a raw-token leak: this is a Phase 16 BLOCKER. Re-open the originating plan, fix the console.log / write call, re-run verification. Per CLAUDE.md §1 + §13 this is the load-bearing AC8 check.

    Acceptable findings:
    - The bootstrap banner contains the bootstrap OTP (D-02 / §13 documented exception).
    - The boot log of "Loaded N paired device(s)" — non-sensitive metadata.
    - The auth-gate log lines (if any) reference deviceId only — opaque public identifier.

    Unacceptable findings (BLOCKERS):
    - Any console.log including the raw token from /pair/redeem.
    - Any on-disk file containing the raw token.
    - The user-minted OTP appearing in stdout (the bootstrap one is the carved exception; user-minted is NOT).
  </action>
  <verify>
    <automated>test -f /tmp/16-ac8-console.log &amp;&amp; wc -l /tmp/16-ac8-console.log &amp;&amp; ls /tmp/16-ac8-stdout.log /tmp/16-ac8-disk-grep.log 2>/dev/null &amp;&amp; echo "audit logs present"</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/16-ac8-console.log captured; every entry classified as acceptable (D-02 exception) or non-token-bearing
    - Live-server audit confirms NO raw token on disk (the grep returns 0 hits across DATADIR)
    - User-minted OTPs do NOT appear in stdout
    - The bootstrap OTP DOES appear in stdout (D-02 expected; documented as the only exception)
    - bootstrap.otp file deleted after first successful redeem
  </acceptance_criteria>
  <done>AC8 audit complete; either CLEAN PASS (no leaks) or BLOCKER (specific file:line of the leak), no third option.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — Lance runs the R3 real-device smoke (canonical mobile gate per Phase 15 D-14 precedent)</name>
  <what-built>
    Phase 16 device pairing is implemented across 16-02 .. 16-07. Vitest + Playwright (where possible) are GREEN per Tasks 1-2. AC8 token-hygiene is clean per Task 3.

    The remaining gate is the R3-precedent **real-mobile smoke** that Phase 15 (15-VERIFICATION.md TL;DR) established as canonical: Playwright iPhone/Android emulation does NOT reliably reflect actual mobile UX (the Phase 15 mobile-viewport / mobile-touch specs failed under emulation but the real-device run on Lance's hardware passed).

    Phase 16 inherits that precedent. The pair flow, the localStorage durability, the WS subprotocol over the OpenVPN-fronting reverse proxy, the 4401 propagation through the proxy, and the iOS Safari ITP behaviour (RESEARCH §5 A3) all need an actual phone to verify.
  </what-built>
  <how-to-verify>
    Lance to run, in this order, with notes captured in the SUMMARY checkbox per item:

    1. **Deploy Phase 16 to your real clideck-docker-lance instance over the VPN.** (Per CLAUDE.md §7: deployment target varies; verify the target before pushing.)
    2. **Fresh phone (or fresh browser context on your phone):** Visit https://<your-clideck-host>/ over the VPN. Expected: redirected to /pair (AC1). No dashboard rendered. No WS established.
    3. **Bootstrap pair (AC7):** SSH to the server, `cat ~/.clideck/bootstrap.otp`. Paste into the phone's /pair form, enter a label ("Lance Phone Test"), submit. Expected: success → dashboard loads → live WS observable (sessions visible).
    4. **AC3 silent reconnect:** Pull-to-refresh the dashboard on the phone. Expected: dashboard reloads without any redirect to /pair, no extra round-trip.
    5. **AC4 unknown token reject:** In Chrome DevTools (or Safari Develop menu), `localStorage.setItem('clideck.deviceToken', 'garbage-not-a-real-token')`, then reload. Expected: WS handshake fails → onclose hybrid fires (1006-with-token) → localStorage cleared → redirect to /pair.
    6. **AC5 revoke from desktop:** Re-pair the phone (steps 3-4). On desktop clideck, Settings → Linked devices → Revoke the phone's row. Confirm the D-06 "other device" copy ("will be signed out immediately"). Confirm. Expected: phone's WS closes with code 4401 within 1s (visible in DevTools Network → WS), phone redirects to /pair, localStorage cleared.
    7. **AC5 self-revoke from phone:** Pair the phone again. On the phone, Settings → Linked devices → Revoke the "This device" row. Confirm the stronger D-06 "this device" copy ("active session list will close"). Confirm. Expected: phone redirects to /pair within 1s.
    8. **AC6 re-pair after revoke:** Mint a fresh OTP (SSH + restart server with empty devices.json, OR via the desktop's Settings if a "+ New code" path exists). Pair the phone with the fresh OTP. Expected: success, no permanent block.
    9. **iOS Safari ITP smoke (RESEARCH §5 A3):** Leave the phone un-touched for 8 days. Then attempt to open the dashboard. If localStorage was cleared, document the behaviour and the recovery path (re-OTP). If localStorage survived (PWA install path), confirm Phase 17 IndexedDB upgrade is NOT urgent. This step is optional/long-running — document the result whenever it lands.
    10. **AC8 deep-dive:** Tail the server log during the pair flow (`docker logs -f clideck` or `journalctl -fu clideck`). Confirm NO raw token appears in any log line. Confirm only the bootstrap OTP (the D-02 exception) appears.

    For each step: capture pass / fail / observation into the SUMMARY's R3 section.

    **DO NOT** paste any token, OTP, or device-fingerprint VALUES into the SUMMARY or commit. Reference by AC + path + "PASS" / "FAIL" / observation prose only. Per CLAUDE.md §13.
  </how-to-verify>
  <resume-signal>Type "approved" with notes per step, OR "blocked: <step N> failed because ..." with a description of the failure for the originating plan to address.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write 16-VERIFICATION.md — mirror Phase 15's structure with the 9 ACs + token-hygiene section + R3 results</name>
  <files>.planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md</files>
  <read_first>
    - @.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md — structural template.
    - /tmp/16-vitest-final.log, /tmp/16-playwright-final.log, /tmp/16-ac8-*.log — Tasks 1-3 captures.
    - The checkpoint outcome from Task 4 (Lance's R3 notes).
    - All 7 16-NN-SUMMARY.md files written by 16-02 .. 16-07.
  </read_first>
  <behavior>
    Create 16-VERIFICATION.md following Phase 15's section structure, adapted for Phase 16's 9 ACs:

    1. **Header** — Phase 16, branch (main), final commit SHA, verified-by (Samuel Harding + Lance for R3).

    2. **TL;DR** — one paragraph. Vitest pass count (e.g. "N/N GREEN across M test files"). Playwright pass count OR "DEFERRED — Chromium libs missing on this WSL host per Phase 15 precedent; pair-flow + revoke-flow validated via Lance's R3 manual gate". AC8 audit result. Any flakes triaged.

    3. **Acceptance Criteria** — 9-row table (one row per AC) with columns: # | SPEC AC (verbatim) | Status | Evidence. Status uses the taxonomy from <interfaces>. Evidence cites file:line + test name + log file.

    4. **AC8 Token-Hygiene Audit** — dedicated section reproducing the Task 3 grep + persistence audit results. Acceptable findings called out (bootstrap banner per D-02 exception); leaks (if any) called out as BLOCKERS. The persistence audit table shows: file | searched-for | matches.

    5. **Vitest Results** — file count, total it-blocks, per-Phase-16-spec breakdown (7 new specs), regression baseline (existing suite still green).

    6. **Playwright Results** — pair-flow + revoke-flow status + any pre-existing flake triage per Phase 15 precedent (Phase 11 ctrl-v-paste flake; session-indicator-mutex idle-working etc. — these will surface again under Phase 16's run and should be flagged as PRE-EXISTING and NOT a Phase 16 regression).

    7. **R3 Real-Mobile Smoke (Lance)** — checklist mirroring Task 4's 10 steps with Lance's pass/fail/observation per step.

    8. **Follow-ups / Phase 17 Candidates** — surfaced from the prior plans' honest-gaps sections:
       - Atomic-write for devices.json + sessions.json + config.json retrofit (16-02 SUMMARY).
       - touchLastSeen debouncer via sessions.startAutoSave-style interval (16-02 SUMMARY).
       - iOS Safari ITP IndexedDB upgrade IF Lance observes silent un-pairs (16-06 SUMMARY).
       - "+ New code" / mint-OTP UI button (16-07 SUMMARY).
       - Inline label editing (16-07 SUMMARY).
       - Per-device permissions (CONTEXT "Deferred Ideas").
       - Labelled other-client indicator (D-05 deferred).

    9. **Phase 15 Merge-Order Note** — confirm whether Phase 15 was merged before / during / after Phase 16; if after, document any post-merge re-grep that was needed (or that is still pending for an executor to run before the next phase plans).

    Per CLAUDE.md §1: every "PASS" claim references actual log output / file:line evidence. No "should pass" / "looks correct" language anywhere. Per §13: no token / OTP values reproduced (redact to `<43-char base64url>` or `<6-char OTP>`).
  </behavior>
  <action>
    1. Open all 7 16-NN-SUMMARY.md files (or capture the key findings as you go through them).
    2. Open the /tmp/16-*.log captures.
    3. Use Write to create `.planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md` following Phase 15's structure as documented above.
    4. Ensure each AC row has FILE:LINE or LOG-PATH evidence, not just "passed".
    5. The TL;DR captures both the automated suite results AND the R3 manual gate outcome from Task 4's checkpoint.

    Per CLAUDE.md §6: this IS the structured completion report for Phase 16. The "Manual testing you can do" + "Testing gaps" sections from §6 are subsumed by the AC matrix + Follow-ups + R3 sections in 16-VERIFICATION.md.
  </action>
  <verify>
    <automated>test -f .planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md &amp;&amp; grep -c '| AC' .planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md &amp;&amp; grep -c '## ' .planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 16-VERIFICATION.md exists
    - Contains a 9-row AC matrix table (`grep -cE '^\\| (AC)?[0-9]+' 16-VERIFICATION.md` returns at least 9)
    - Contains all required sections: TL;DR, Acceptance Criteria, Token-Hygiene Audit, Vitest, Playwright, R3 Real-Mobile, Follow-ups, Phase 15 Merge-Order
    - Each AC row has evidence beyond a bare "PASS" — file:line, test name, or log path
    - No raw tokens / OTP values reproduced verbatim (CLAUDE.md §13)
    - The R3 section reflects Lance's actual checkpoint notes (not placeholder text)
  </acceptance_criteria>
  <done>16-VERIFICATION.md exists with all 9 ACs mapped to evidence; suite results captured; R3 results captured; follow-ups enumerated; the document stands as the canonical close-out of Phase 16.</done>
</task>

</tasks>

<verification>
After all 5 tasks (4 auto + 1 checkpoint) land:

```bash
test -f .planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md
grep -E "^\| (AC)?[0-9]" .planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md | wc -l
# Expected: at least 9 AC rows
```

The phase commits its 16-VERIFICATION.md and Phase 16 is done. The next phase (17?) inherits the
follow-ups list documented in section 8.

Per CLAUDE.md §3: do NOT push the verification commit (GitHub remote). Lance reviews and pushes
manually when satisfied.
</verification>

<success_criteria>
- [ ] Vitest run executed; results captured to /tmp/16-vitest-final.log
- [ ] Playwright run attempted (or Chromium-libs deferral documented per Phase 15 precedent)
- [ ] AC8 token-hygiene audit clean — no raw token in code logs or persistence
- [ ] Lance's R3 real-mobile smoke completed via checkpoint (Task 4)
- [ ] 16-VERIFICATION.md written with all 9 ACs mapped to evidence
- [ ] No silent skips, no "should pass" language, no token values reproduced
- [ ] Commit lands on `main`, NOT pushed (CLAUDE.md §3)
- [ ] Phase 16 closes out cleanly; follow-ups handed to Phase 17 candidate list
</success_criteria>

<output>
Create `.planning/2026-06-05-device-pairing-for-mobile-access/16-08-SUMMARY.md` AND
`.planning/2026-06-05-device-pairing-for-mobile-access/16-VERIFICATION.md`.

The SUMMARY captures the meta-process (what was run, what was deferred, what was flaky).
The VERIFICATION is the canonical AC-by-AC document Lance reads to know "Phase 16 done?".
</output>
