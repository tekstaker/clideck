---
phase: 16-device-pairing-for-mobile-access
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/pair-otp.test.js
  - tests/devices-json.test.js
  - tests/pair-redeem.test.js
  - tests/ws-auth-gate.test.js
  - tests/revoke-closes-socket.test.js
  - tests/bootstrap-otp.test.js
  - tests/device-revoke-rebuild.test.js
  - e2e/pair-flow.spec.js
  - e2e/revoke-flow.spec.js
autonomous: true
requirements: [AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8, AC9]
must_haves:
  truths:
    - "Every behaviour-adding task in Waves 1-3 has a RED test authored in this plan first (CLAUDE.md §2)"
    - "All 7 vitest unit specs fail when run today (exit code 1) because the modules under test do not yet exist"
    - "Both Playwright specs fail today because /pair, the WS auth gate, and the Settings devices panel are not yet implemented"
    - "Each RED reason is documented in the test file's header comment so a checker can verify intent vs. accidental green"
    - "Test-data-dir isolation uses CLIDECK_DATA_DIR=mkdtempSync(...) per PATTERNS §4.2 — no test pollutes ~/.clideck/"
  artifacts:
    - path: tests/pair-otp.test.js
      provides: "OTP mint/expiry/single-use contract (R-2, AC9)"
      contains: "@vitest-environment node"
    - path: tests/devices-json.test.js
      provides: "devices.json schema + plain writeFileSync round-trip; raw token never persists (R-3, AC8)"
      contains: "expect(JSON.stringify(found)).not.toContain(rawToken)"
    - path: tests/pair-redeem.test.js
      provides: "Pair-redeem HTTP handler over fake req/res — happy path + expired + reused + malformed (AC2, AC9)"
      contains: "describe('POST /pair/redeem'"
    - path: tests/ws-auth-gate.test.js
      provides: "verifyClient reject for absent/unknown token, accept for known (AC4)"
      contains: "expect(verified).toBe(false)"
    - path: tests/revoke-closes-socket.test.js
      provides: "ws.close(4401, 'revoked') fires for matching ws.deviceTokenHash within 1s (AC5)"
      contains: "closeDevice"
    - path: tests/bootstrap-otp.test.js
      provides: "Empty devices.json → OTP printed to stdout + written to .clideck/bootstrap.otp; deleted on first redeem (D-02, AC7)"
      contains: "BOOTSTRAP_PATH"
    - path: tests/device-revoke-rebuild.test.js
      provides: "After revoke, fresh OTP → re-pair succeeds (AC6)"
      contains: "re-pair"
    - path: e2e/pair-flow.spec.js
      provides: "Empty localStorage → /pair → enter OTP → success → reload to dashboard (AC1, AC2, AC3)"
      contains: "page.goto('/pair')"
    - path: e2e/revoke-flow.spec.js
      provides: "Settings → Revoke other device → WS close 4401 observed → /pair view (AC5)"
      contains: "device.revoke"
  key_links:
    - from: tests/pair-otp.test.js
      to: pair-otp.js (NEW in Wave 1)
      via: "require('../pair-otp.js')"
      pattern: "freshDevices\\(\\) / freshOtp\\(\\) require-cache wipe"
    - from: tests/ws-auth-gate.test.js
      to: server.js verifyClient 2-arg form
      via: "extracting the verifyClient handler for direct invocation OR booting the server module"
      pattern: "verifyClient.length === 2"
    - from: tests/revoke-closes-socket.test.js
      to: sessions.js closeDevice helper (NEW in Wave 2)
      via: "extend captureClient fake to record close([code, reason])"
      pattern: "sessions.closeDevice|sessions.clients iteration"
---

<objective>
Wave 0 — author 7 vitest unit specs + 2 Playwright e2e specs as RED-state contracts for the 9 ACs.
Per CLAUDE.md §2 every behaviour-adding task in Waves 1-3 has its test authored here first. Each test
deliberately fails today: the modules / routes / panels it targets do not exist yet. Wave 1-3 plans
turn these specs green by building the implementations they describe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-05-device-pairing-for-mobile-access/SPEC.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-CONTEXT.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md
@.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md

# Verified analogs (read before authoring tests)
@tests/check-cwd-handler.test.js
@tests/resumable-handlers.test.js
@tests/session-pause.test.js
@e2e/smoke.spec.js
@e2e/session-pause.spec.js
@vitest.config.js
@playwright.config.js
@paths.js
</context>

<interfaces>
<!-- Verified-against-HEAD contracts the Wave 0 tests will reference. Quoted verbatim
     from the analog files so the executor authors RED tests that exactly mirror the
     project's existing patterns (PATTERNS §4.1-4.4). -->

## vitest config (vitest.config.js:1-9)
Default env: `happy-dom`. Server-side tests MUST start with `// @vitest-environment node`
on line 1 (PATTERNS §4.1).

## test-data-dir isolation (PATTERNS §4.2, tests/check-cwd-handler.test.js:44-55)
```
beforeEach(() => {
  TEST_DATA_DIR = mkdtempSync(join(tmpdir(), 'clideck-<feature>-test-'));
  process.env.CLIDECK_DATA_DIR = TEST_DATA_DIR;
});
afterEach(() => {
  delete process.env.CLIDECK_DATA_DIR;
  vi.restoreAllMocks();
  try { rmSync(TEST_DATA_DIR, { recursive: true, force: true }); } catch {}
});
```
paths.js:5 reads CLIDECK_DATA_DIR, so the env-var swap relocates ~/.clideck/ for the test.

## Fresh-require-cache helper (PATTERNS §4.3, tests/check-cwd-handler.test.js:24-32)
```
function freshHandlers() {
  for (const k of Object.keys(require.cache)) {
    if (k.includes(`${require('path').sep}clideck${require('path').sep}`) &&
        !k.includes('node_modules')) {
      delete require.cache[k];
    }
  }
  return require('../handlers.js');
}
```
Rename per-target: `freshDevices()`, `freshOtp()`, `freshPairRedeem()`, `freshSessions()`, etc.

## Fake-ws factory (PATTERNS §4.4, tests/check-cwd-handler.test.js:34-42)
```
function fakeWs() {
  const ws = new EventEmitter();
  ws.readyState = 1;
  ws.sent = [];
  ws.send = (raw) => { try { ws.sent.push(JSON.parse(raw)); } catch { ws.sent.push(raw); } };
  ws.ping = () => {};
  ws.terminate = () => {};
  return ws;
}
```
Wave 0 extension (RESEARCH §10.6, PATTERNS §4.4): also record close:
```
ws.close = (code, reason) => { ws.sent.push({ __close: [code, reason] }); ws.readyState = 3; };
```

## Playwright e2e helpers (e2e/smoke.spec.js:14-53)
`installWsRecorder(page)` patches WebSocket to record sent + received types onto window globals
(`__rxTypes`, `__sentMessages`, `__ws`). Used to assert post-pair the WS connects and the
expected broadcast types (`config`, `sessions`, `presets`) arrive.

`waitForAppReady(page)` polls until `__rxTypes` includes `['config', 'sessions', 'presets']`.

Playwright config (playwright.config.js): isolated HOME tempdir so the test server's
~/.clideck/ is clean. Port 4099. `node server.js` is the webServer command.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/pair-otp.test.js — OTP mint, TTL, single-use (RED)</name>
  <files>tests/pair-otp.test.js</files>
  <read_first>
    - @tests/check-cwd-handler.test.js lines 1-55 — `// @vitest-environment node` directive, `freshHandlers()` cache wipe, `fakeWs()` factory, beforeEach/afterEach tempdir.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.3 — the OTP store shape (`mintOtp`, `redeemOtp` return values; alphabet; 5-min TTL).
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md §4.1-4.3 — vitest-environment node, tempdir isolation, freshDevices/freshOtp idiom.
  </read_first>
  <behavior>
    RED reason: `../pair-otp.js` (or wherever Wave 1 plan 16-03 lands the OTP module) does not exist yet. `freshOtp()` will throw MODULE_NOT_FOUND.

    Cases (all expected to fail today; all expected to pass after 16-03 lands):
    - mintOtp() returns { otp: <6-char string from OTP_ALPHABET>, expiresAt: <ISO string> }
    - generated OTP only uses chars from `ABCDEFGHJKMNPQRSTUVWXYZ23456789` (31-char alphabet per Q-1 / D-04 / planner pin; no 0/O/1/I/L)
    - redeemOtp(otp) returns { ok: true, isBootstrap: false } on first call for a freshly-minted OTP
    - redeemOtp(otp) returns { ok: false, error: 'used' } on second call with the same OTP (AC9 single-use)
    - redeemOtp(<unknown>) returns { ok: false, error: 'invalid' }
    - redeemOtp(<expired>) returns { ok: false, error: 'expired' } when fast-forwarded past ttlSeconds (use `vi.useFakeTimers()` + `vi.advanceTimersByTime(6 * 60 * 1000)`)
    - OTP comparison is case-insensitive: `redeemOtp(otp.toLowerCase())` succeeds (mirrors `/pair/redeem` body normalisation per RESEARCH §10.4)
  </behavior>
  <action>
    Create tests/pair-otp.test.js mirroring the analog at @tests/check-cwd-handler.test.js. Top line MUST be `// @vitest-environment node`. Include a header comment naming this as a Wave-0 RED-state TDD spec for Phase 16, citing AC9 and RESEARCH §10.3. Implement `freshOtp()` that wipes the require cache (clideck-paths-only, NOT node_modules, per PATTERNS §4.3) and `return require('../pair-otp.js');`.

    Author 7 `it(...)` blocks covering the cases in the <behavior> block above. Use `vi.useFakeTimers()` / `vi.advanceTimersByTime()` for the expiry test. Use `expect(otp).toMatch(/^[ABCDEFGHJKMNPQRSTUVWXYZ23456789]{6}$/)` for the alphabet test. Use `process.env.CLIDECK_DATA_DIR = mkdtempSync(...)` even though OTPs are in-memory only — the require cache wipe will pull `paths.js` into the test boot and we want it to mkdir into the tempdir not the user's real home (defence per PATTERNS §4.2). Cleanup in afterEach.
  </action>
  <verify>
    <automated>npx vitest run tests/pair-otp.test.js --reporter=verbose 2>&amp;1 | tail -40; echo "expected: exit 1, Cannot find module '../pair-otp.js'"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/pair-otp.test.js`
    - First line is the vitest env directive: `head -n 1 tests/pair-otp.test.js | grep -F '// @vitest-environment node'`
    - Contains 7 it-blocks: `grep -c "^\s*it(" tests/pair-otp.test.js` returns 7
    - Contains the alphabet assertion: `grep -F 'ABCDEFGHJKMNPQRSTUVWXYZ23456789' tests/pair-otp.test.js`
    - Contains the require-cache wipe: `grep -F "delete require.cache[k]" tests/pair-otp.test.js`
    - `npx vitest run tests/pair-otp.test.js` exits non-zero (RED)
  </acceptance_criteria>
  <done>pair-otp.test.js exists, fails today with MODULE_NOT_FOUND, and is the contract Wave 1 plan 16-03 must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/devices-json.test.js — schema + round-trip + raw-token never persisted (RED)</name>
  <files>tests/devices-json.test.js</files>
  <read_first>
    - @tests/resumable-handlers.test.js lines 1-46 — tempdir isolation, freshSessionsModule idiom.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.2 — devices.js exports (load, save, list, add, remove, findByToken, mintToken, hashToken, DEVICES_PATH, BOOTSTRAP_PATH).
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.8 — example shape for the round-trip test.
    - @paths.js — DATA_DIR derives from CLIDECK_DATA_DIR env var.
  </read_first>
  <behavior>
    RED reason: `../devices.js` does not exist yet. `freshDevices()` throws MODULE_NOT_FOUND.

    Cases:
    - load() on empty DATA_DIR initialises in-memory store to `{ version: 1, devices: [] }`
    - add({ label, uaFingerprint, rawToken }) appends a record with `id` starting `dev_` and a sha256-prefixed token_hash; returns the record
    - The returned record contains `paired_at` and `last_seen` as ISO timestamps that round-trip through Date.parse
    - After add(), `JSON.parse(readFileSync(DEVICES_PATH, 'utf8')).devices.length === 1`
    - **AC8 invariant:** The raw token never appears in the on-disk file. `expect(readFileSync(DEVICES_PATH, 'utf8')).not.toContain(rawToken)`
    - hashToken(raw) returns `sha256:<64 hex chars>` (length 71 total — verifies the safeEqualHash length guard)
    - findByToken(rawToken) returns the record after add(); findByToken('<wrong>') returns null; findByToken('') returns null; findByToken(null) returns null
    - findByToken uses constant-time compare (sanity check: a second store loaded fresh from disk via freshDevices() + load() still finds the token — proves the hash, not the raw, is the lookup key)
    - remove(deviceId) removes the row, returns 1; remove('dev_nonexistent') returns 0
    - touchLastSeen(id) updates the in-memory record AND writes to disk (assert by reloading freshly)
    - The on-disk file uses plain `writeFileSync` (no `.tmp` file created) — Option A pin per planner decision (PATTERNS §2.2). Assert by snapshotting the DEVICES_PATH dir contents during/after a save: only `devices.json` exists, no `devices.json.tmp-*`.
  </behavior>
  <action>
    Create tests/devices-json.test.js. Line 1: `// @vitest-environment node`. Header comment: Wave 0 RED-state, cites AC8 (token-never-leaks) and PATTERNS §4.2/§4.3. Implement `freshDevices()` per the freshHandlers pattern. beforeEach mkdtempSync's into `clideck-devices-test-`; afterEach rmSync's.

    Use a non-secret string for the test token (e.g. `const rawToken = 'test-token-NOT-real-' + Math.random().toString(36).slice(2);`). Per CLAUDE.md §13 do not paste an actual high-entropy token into the test file — synthetic is fine for proving the not-toContain invariant.

    For the "no .tmp file exists" assertion: after each save, `readdirSync(TEST_DATA_DIR).filter(f =&gt; f.startsWith('devices.json')).length === 1` and the entry is exactly `devices.json`.

    Author 9 it-blocks covering the cases listed in <behavior>.
  </action>
  <verify>
    <automated>npx vitest run tests/devices-json.test.js --reporter=verbose 2>&amp;1 | tail -40; echo "expected: exit 1, MODULE_NOT_FOUND ../devices.js"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the vitest env directive on line 1
    - `grep -c "^\s*it(" tests/devices-json.test.js` returns 9
    - Contains the AC8 invariant: `grep -F 'not.toContain(rawToken)' tests/devices-json.test.js`
    - Contains the no-tmp-file assertion: `grep -F "startsWith('devices.json')" tests/devices-json.test.js`
    - Contains the sha256-prefix assertion: `grep -E "sha256:.{64}|/\\^sha256:" tests/devices-json.test.js`
    - `npx vitest run tests/devices-json.test.js` exits non-zero
  </acceptance_criteria>
  <done>devices-json.test.js authored, RED, contracts the devices.js module that 16-02 will build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author tests/pair-redeem.test.js — /pair/redeem HTTP handler (RED)</name>
  <files>tests/pair-redeem.test.js</files>
  <read_first>
    - @tests/check-cwd-handler.test.js — fake-ws + req/res mocking pattern (NOTE: PATTERNS §1 row 7 calls out that there is NO existing HTTP-handler unit test in the suite, so this test is the first; the shape below is novel-for-clideck).
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.4 — the /pair/redeem handler implementation that 16-04 will splice into server.js.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md §1 rows 6-7 — "POST /pair/redeem JSON handler" analog points at session-ask.js handleHttp shape.
  </read_first>
  <behavior>
    RED reason: the /pair/redeem handler does not exist in server.js yet. Wave 1 plan 16-04 will extract the handler to a `routes/pair.js` (or equivalent) module exposing `handleRedeemHttp(req, res, deps)` where `deps = { devices, pairOtp }` is injected. This test imports that handler directly.

    Cases (with mocked req/res EventEmitters):
    - Happy path: POST a valid OTP → handler responds 200 JSON `{ ok: true, device_id: 'dev_*', token: '<43-char base64url>', label: 'Test Phone' }`. devices.list().length === 1 afterward.
    - Expired OTP: returns 410 JSON `{ ok: false, error: 'expired' }` (per RESEARCH §10.4 status mapping)
    - Used OTP (second redeem of same OTP): returns 400 JSON `{ ok: false, error: 'used' }`
    - Unknown OTP: returns 400 JSON `{ ok: false, error: 'invalid' }`
    - Malformed JSON body: returns 400 JSON `{ ok: false, error: 'invalid-json' }`
    - Body cap: posting > 4KB body destroys the request (no response, request emits 'close')
    - Label normalisation: empty label → 'Device'; 60-char label → truncated to 32 trimmed chars
    - **AC8:** the raw token is NOT logged to console during the redeem. Assert via `vi.spyOn(console, 'log')` capturing zero calls that include the raw-token string.
    - **AC8:** the raw token IS returned in the response body exactly once; immediately after, `devices.list()[0].token_hash` starts with `sha256:` and does NOT contain the raw token substring.
    - Bootstrap flag clears bootstrap.otp file: when redeeming the bootstrap OTP, `existsSync(devices.BOOTSTRAP_PATH)` is false afterward.
  </behavior>
  <action>
    Create tests/pair-redeem.test.js. Line 1: `// @vitest-environment node`. Header: Wave 0 RED for AC2, AC8, AC9 and the /pair/redeem dispatcher; flags that this is clideck's first HTTP-handler unit test (cites PATTERNS §1 row 7).

    Implement a `mockReq({ method='POST', url='/pair/redeem', body, headers={} } = {})` helper that returns an EventEmitter and exposes `req.headers`, `req.method`, `req.url`; the test feeds the body via `req.emit('data', Buffer.from(JSON.stringify(payload)))` then `req.emit('end')`. Implement `mockRes()` that records `writeHead(status, headers)` and `end(body)` into an `out` object the test asserts on.

    Implement `freshPair()` that wipes the clideck require-cache and returns `{ devices: require('../devices.js'), pairOtp: require('../pair-otp.js'), pairRoute: require('../routes/pair.js') }`. Wave 1 plan 16-04 decides between `routes/pair.js` (recommended) vs inlining into server.js; if 16-04 inlines, this test will need to extract the handler — the test asserts the contract regardless.

    Author 10 it-blocks. Use the `mintOtp({ ttlSeconds: 300 })` from the OTP module to set up the OTP, then drive the handler. For the expired test, use `vi.useFakeTimers()` and advance past 5min.
  </action>
  <verify>
    <automated>npx vitest run tests/pair-redeem.test.js --reporter=verbose 2>&amp;1 | tail -40; echo "expected: exit 1, MODULE_NOT_FOUND ../routes/pair.js (or similar)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with vitest env directive
    - `grep -c "^\s*it(" tests/pair-redeem.test.js` returns 10
    - Contains the mockReq/mockRes helpers: `grep -E "function mockReq|function mockRes" tests/pair-redeem.test.js` returns at least 2 lines
    - Asserts 410 for expired: `grep -F "writeHead).*410\|status.*410" tests/pair-redeem.test.js`
    - Asserts AC8 token-not-in-logs: `grep -F "spyOn(console, 'log')" tests/pair-redeem.test.js`
    - `npx vitest run tests/pair-redeem.test.js` exits non-zero
  </acceptance_criteria>
  <done>pair-redeem.test.js authored, RED, contracts the /pair/redeem route handler 16-04 will build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Author tests/ws-auth-gate.test.js — verifyClient reject/accept (RED)</name>
  <files>tests/ws-auth-gate.test.js</files>
  <read_first>
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §1 + §7 + §10.1 — the verifyClient 2-arg form, the readDeviceToken helper, and the server.js:366-380 splice.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md §2.1 — handleProtocols vs verifyClient splice notes.
    - @server.js lines 366-371 — current verifyClient is the 1-arg origin check `({ req }) =&gt; isAllowedWsOrigin(...)`. Verify the line numbers against the current HEAD; PATTERNS.md was pinned to commit `b4a09fb` and Phase 15's eventual merge may shift them. Re-grep `verifyClient` before authoring assertions against exact lines.
  </read_first>
  <behavior>
    RED reason: server.js still has the 1-arg `verifyClient` and no devices.js to look up against. The test imports the auth-gate factory (Wave 2 plan 16-05 must expose `makeVerifyClient({ devices, isAllowedWsOrigin })` returning the 2-arg callback shape).

    Cases:
    - Missing Sec-WebSocket-Protocol header → callback(false, 401, 'unpaired')
    - Sec-WebSocket-Protocol present but only contains the sentinel `clideck-device-token` (no token) → callback(false, 401, 'unpaired')
    - Sec-WebSocket-Protocol carries an unknown token → callback(false, 401, 'unpaired')
    - Origin disallowed → callback(false, 403, 'origin not allowed') (preserves existing isAllowedWsOrigin check, runs before token check)
    - Valid token in subprotocol → callback(true) AND `req.clideckDevice.id === <pre-added device id>`
    - **req.clideckDevice is stashed for the onConnection wrapper** (per RESEARCH §10.1): asserts `req.clideckDevice.token_hash` is set after a successful verify
    - readDeviceToken handles whitespace and ordering tolerance: `'clideck-device-token, abc'`, `'abc , clideck-device-token'`, `'clideck-device-token,abc'` all extract 'abc'
    - Malformed subprotocol headers (e.g. `,,,`) don't throw — return null token → reject
  </behavior>
  <action>
    Create tests/ws-auth-gate.test.js. Line 1: `// @vitest-environment node`. Header: Wave 0 RED for AC4. Cites RESEARCH §7 and §10.1.

    `freshGate()` wipes cache and `return require('../auth-gate.js');` (Wave 2 plan 16-05 may name this differently; the test references the symbolic path and 16-05 must export `makeVerifyClient` and `readDeviceToken` from a single module the test can import — pin that decision now). If 16-05 prefers inlining into server.js, it must still export the two helpers from a top-level require so tests can drive them in isolation.

    Build a `fakeReq({ origin, host, subprotocol })` helper that returns `{ headers: { origin, host, 'sec-websocket-protocol': subprotocol } }`.

    For the happy-path test, set up a real devices module: `mkdtempSync` a tempdir, set CLIDECK_DATA_DIR, `devices.load()`, then call `devices.add({ label: 'Test', uaFingerprint: 'ua-x', rawToken: 'test-token-NOT-real-' + Math.random().toString(36).slice(2) })`, capture the rawToken, then drive verifyClient with `subprotocol: 'clideck-device-token, ' + rawToken`.

    Author 8 it-blocks.
  </action>
  <verify>
    <automated>npx vitest run tests/ws-auth-gate.test.js --reporter=verbose 2>&amp;1 | tail -40; echo "expected: exit 1, MODULE_NOT_FOUND ../auth-gate.js"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with vitest env directive
    - `grep -c "^\s*it(" tests/ws-auth-gate.test.js` returns 8
    - Asserts the 2-arg callback signature: `grep -F 'callback(false, 401' tests/ws-auth-gate.test.js`
    - Asserts req.clideckDevice stashing: `grep -F 'req.clideckDevice' tests/ws-auth-gate.test.js`
    - `npx vitest run tests/ws-auth-gate.test.js` exits non-zero
  </acceptance_criteria>
  <done>ws-auth-gate.test.js authored, RED, contracts the 16-05 makeVerifyClient + readDeviceToken exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Author tests/revoke-closes-socket.test.js — ws.close(4401, 'revoked') fires within 1s (RED)</name>
  <files>tests/revoke-closes-socket.test.js</files>
  <read_first>
    - @tests/session-pause.test.js lines 49-57 — `captureClient(sessions)` helper that pushes a fake ws into sessions.clients.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.6 — revoke handler shape iterating sessions.clients and calling ws.close(4401, 'revoked').
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md §2.3 — closeDevice(tokenHash) function shape.
    - @sessions.js lines 53-55 (broadcast loop) — the analog iteration over `clients`. Verify the line numbers against the current HEAD; PATTERNS.md was pinned to commit `b4a09fb` and Phase 15's eventual merge may shift them. Re-grep `function broadcast` before authoring assertions.
  </read_first>
  <behavior>
    RED reason: sessions.js does not yet export `closeDevice(tokenHash | deviceId)`. The revoke message handler (Wave 2 plan 16-05 / 16-07) does not yet exist.

    Cases:
    - With 3 fake clients in sessions.clients — two share ws.deviceId='dev_A', one is ws.deviceId='dev_B' — calling sessions.closeDevice('dev_A') closes both dev_A clients with code 4401, reason 'revoked', and leaves dev_B alone.
    - Returns the count closed (2 in the case above).
    - Iteration tolerates a ws whose close() throws — other clients still close (try/catch per-client, mirror PATTERNS §2.3).
    - Closed clients see their fake `__close` recorded as `[4401, 'revoked']`.
    - Within 1s: the loop is synchronous; use `Date.now()` before/after to assert &lt; 1000ms for 50 fake clients (proves AC5 latency claim).
    - Calling closeDevice('dev_doesnotexist') returns 0 and closes nothing.
    - sessions.broadcast({ type: 'device.revoked', deviceId: 'dev_A' }) is observable on the surviving dev_B client's `sent` array after the full revoke handler runs (this is exercised in the integration-style sub-test that drives the handler from the handlers.js message switch — see Wave 2 plan 16-05 contract).
  </behavior>
  <action>
    Create tests/revoke-closes-socket.test.js. Line 1: `// @vitest-environment node`. Header: Wave 0 RED for AC5.

    Extend the fakeWs factory per PATTERNS §4.4: include `close: (code, reason) => { ws.sent.push({ __close: [code, reason] }); ws.readyState = 3; }`. Set `ws.deviceId` and `ws.deviceTokenHash` directly on each fake instance (Pattern A from RESEARCH §4).

    `freshSessions()` cache-wipes and returns `require('../sessions.js')`. The test directly mutates `sessions.clients.add(fakeWs)` (mirrors the existing handlers.js:267 splice point per PATTERNS §1 row 9).

    Author 7 it-blocks. For the latency test, create 50 fake clients in a loop, all with `ws.deviceId='dev_X'`, call closeDevice('dev_X'), assert all 50 sent={__close:[4401,'revoked']} and the elapsed wall-clock is &lt; 1000.
  </action>
  <verify>
    <automated>npx vitest run tests/revoke-closes-socket.test.js --reporter=verbose 2>&amp;1 | tail -40; echo "expected: exit 1, sessions.closeDevice is not a function"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with vitest env directive
    - `grep -c "^\s*it(" tests/revoke-closes-socket.test.js` returns 7
    - Asserts the close-code: `grep -F '4401' tests/revoke-closes-socket.test.js`
    - Asserts the latency budget: `grep -E 'toBeLessThan.*1000|< 1000' tests/revoke-closes-socket.test.js`
    - `npx vitest run tests/revoke-closes-socket.test.js` exits non-zero
  </acceptance_criteria>
  <done>revoke-closes-socket.test.js authored, RED, contracts sessions.closeDevice and the revoke broadcast.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Author tests/bootstrap-otp.test.js — empty devices.json → stdout + .clideck/bootstrap.otp, deleted on redeem (RED)</name>
  <files>tests/bootstrap-otp.test.js</files>
  <read_first>
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-CONTEXT.md D-02 — owner-bootstrap pin (boot OTP to stdout + .clideck/bootstrap.otp, deleted on first successful redeem).
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.3 — `bootstrapIfNeeded()` shape including the stdout banner.
    - @paths.js — DATA_DIR / CLIDECK_DATA_DIR.
  </read_first>
  <behavior>
    RED reason: `pair-otp.js` and `devices.js` and the `bootstrapIfNeeded()` function do not exist.

    Cases:
    - bootstrapIfNeeded() called when devices.isEmpty() → writes a 6-char OTP to `${DATA_DIR}/bootstrap.otp` and logs a `[clideck] bootstrap pair code:` banner to stdout (capture via `vi.spyOn(console, 'log')`).
    - bootstrapIfNeeded() called when devices has ≥1 device → no file written, no banner logged.
    - The OTP in bootstrap.otp matches the OTP visible in the captured console.log call (they're the same OTP — assert by extracting the OTP from the banner and comparing).
    - The bootstrap.otp file content is the 6-char OTP + trailing newline (no extra whitespace).
    - After redeeming the bootstrap OTP via redeemOtp + clearBootstrap, `existsSync(BOOTSTRAP_PATH)` is false.
    - The bootstrap OTP has a long TTL (24h per RESEARCH §10.3) — fast-forward 6 hours, still redeemable.
    - The bootstrap OTP redeems with `{ ok: true, isBootstrap: true }` (distinguishable from a user-minted OTP, which redeems with `isBootstrap: false`).
  </behavior>
  <action>
    Create tests/bootstrap-otp.test.js. Line 1: `// @vitest-environment node`. Header: Wave 0 RED for AC7 + D-02.

    beforeEach tempdir + CLIDECK_DATA_DIR per PATTERNS §4.2.

    Use `vi.spyOn(console, 'log').mockImplementation(() =&gt; {})` to capture stdout. Extract the OTP from the captured banner with a regex on `vi.mocked(console.log).mock.calls` joining the args; assert the same OTP is in `readFileSync(BOOTSTRAP_PATH, 'utf8').trim()`.

    Author 7 it-blocks.
  </action>
  <verify>
    <automated>npx vitest run tests/bootstrap-otp.test.js --reporter=verbose 2>&amp;1 | tail -40; echo "expected: exit 1, MODULE_NOT_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with vitest env directive
    - `grep -c "^\s*it(" tests/bootstrap-otp.test.js` returns 7
    - Asserts the banner format: `grep -F 'bootstrap pair code' tests/bootstrap-otp.test.js`
    - Asserts file deletion: `grep -F 'existsSync(BOOTSTRAP_PATH)' tests/bootstrap-otp.test.js` or `grep -F '.bootstrap.otp' tests/bootstrap-otp.test.js`
    - `npx vitest run tests/bootstrap-otp.test.js` exits non-zero
  </acceptance_criteria>
  <done>bootstrap-otp.test.js authored, RED, contracts the bootstrap discipline in 16-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Author tests/device-revoke-rebuild.test.js — after revoke, fresh OTP re-pairs (RED)</name>
  <files>tests/device-revoke-rebuild.test.js</files>
  <read_first>
    - @.planning/2026-06-05-device-pairing-for-mobile-access/SPEC.md AC6 — "After revoke, the same browser pasting a fresh OTP succeeds. No permanent block — pairing is per-token, not per-browser-fingerprint."
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.3 + §10.4 — mintOtp + redeem + devices.add cycle.
  </read_first>
  <behavior>
    RED reason: the full mint → redeem → revoke → re-mint → re-redeem cycle requires devices.js + pair-otp.js + the /pair/redeem handler. All three are NEW.

    Cases:
    - Full cycle: mintOtp() → add via redeem flow → devices.list().length === 1; remove(id) → devices.list().length === 0; mintOtp() → add via redeem flow → devices.list().length === 1 again with a NEW dev_* id.
    - The new device record's token_hash is DIFFERENT from the revoked one (each pair mints a fresh token).
    - The new device record's paired_at is &gt;= the revoked-and-removed device's paired_at (sanity, with `vi.useFakeTimers()`).
    - Same UA fingerprint can re-pair (proves "per-token, not per-fingerprint" — AC6 explicit).
    - After the second pair, devices.findByToken(oldRawToken) is null (the revoked token is dead); devices.findByToken(newRawToken) returns the new record.
  </behavior>
  <action>
    Create tests/device-revoke-rebuild.test.js. Line 1: `// @vitest-environment node`. Header: Wave 0 RED for AC6.

    Use freshDevices() + freshOtp() + freshPair() pattern. Don't drive the HTTP handler in this test — drive the devices+otp APIs directly to keep the scope tight. The HTTP-level cycle is covered by an e2e spec.

    Author 5 it-blocks.
  </action>
  <verify>
    <automated>npx vitest run tests/device-revoke-rebuild.test.js --reporter=verbose 2>&amp;1 | tail -40; echo "expected: exit 1, MODULE_NOT_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with vitest env directive
    - `grep -c "^\s*it(" tests/device-revoke-rebuild.test.js` returns 5
    - Asserts the per-token (not per-fingerprint) invariant: `grep -E "same.*fingerprint|per-token" tests/device-revoke-rebuild.test.js`
    - `npx vitest run tests/device-revoke-rebuild.test.js` exits non-zero
  </acceptance_criteria>
  <done>device-revoke-rebuild.test.js authored, RED, contracts the re-pair semantics for AC6.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Author e2e/pair-flow.spec.js — empty localStorage → /pair → success → dashboard (RED)</name>
  <files>e2e/pair-flow.spec.js</files>
  <read_first>
    - @e2e/smoke.spec.js — installWsRecorder + waitForAppReady helpers; reuse them.
    - @playwright.config.js — HOME tempdir + PORT 4099 wiring; the e2e server starts with a fresh `.clideck/` per run, so the bootstrap OTP is always available at `${TEST_HOME}/.clideck/bootstrap.otp` for this spec to read.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-CONTEXT.md D-02 — bootstrap OTP file location.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-PATTERNS.md §1 row 19 — flagged that this is the first standalone (non-app-shell) Playwright spec.
  </read_first>
  <behavior>
    RED reason: /pair page doesn't exist, /pair/redeem doesn't exist, the WS subprotocol gate doesn't exist, and the dashboard's localStorage-based redirect doesn't exist.

    Cases (all in one test or split across 3 tests — author's call, but the flow is one journey):
    - Test 1 — fresh-load gating (AC1): page.goto('/') with empty localStorage → page navigates to `/pair` (the dashboard either redirects via JS or the server serves it). Assert page.url() ends with '/pair'. Assert NO WebSocket connects (poll for 1s that `window.__ws` is undefined / null).
    - Test 2 — bootstrap pair end-to-end (AC2, AC7): page.goto('/pair'). Read the bootstrap OTP from the test server's data dir (the playwright config exposes the TEST_HOME path; the test reads `${process.env.TEST_HOME or HOME}/.clideck/bootstrap.otp` via Node fs — Playwright test files are Node, so this works). Fill the OTP input, fill the label input ('E2E Test Phone'), click submit. After the redeem succeeds: assert `localStorage.getItem('clideck.deviceToken')` is a 43-char base64url string; assert page navigates to `/`; assert the WS connects and the standard initial broadcasts arrive (config, sessions, presets) — reuse waitForAppReady.
    - Test 3 — known device reconnects silently (AC3): set localStorage.clideck.deviceToken to the value from Test 2 (or use a context.storageState fixture), page.goto('/'). Assert NO redirect to /pair. Assert dashboard renders (#nav-rail, #session-list visible). Assert the WS connects.
  </behavior>
  <action>
    Create e2e/pair-flow.spec.js. Header comment: Wave 0 RED Playwright spec for AC1, AC2, AC3, AC7. Cites PATTERNS §1 row 19 (first standalone-page e2e in clideck).

    Use `const { test, expect } = require('@playwright/test');` plus `const { readFileSync, existsSync } = require('fs'); const { join } = require('path');`. Helper to find the test server's data dir: `function dataDirFromEnv() { return process.env.CLIDECK_DATA_DIR || join(process.env.HOME || process.env.USERPROFILE, '.clideck'); }`.

    For Test 2, poll for the bootstrap file to exist (server may take a moment on first boot): `await expect.poll(() =&gt; existsSync(join(dataDirFromEnv(), 'bootstrap.otp')), { timeout: 5000 }).toBe(true);` then read it.

    For test isolation: each test's localStorage is fresh by default in Playwright (new context per test). For Test 3, set storage explicitly via `await page.evaluate((tok) =&gt; localStorage.setItem('clideck.deviceToken', tok), capturedToken)` BEFORE the goto.

    Note in the file header: this spec depends on the playwright.config.js's HOME-tempdir trick — the server's bootstrap OTP will be in the per-run tempdir, NOT the user's real ~/.clideck/. Per CLAUDE.md §13 secrets hygiene, do NOT log the OTP value to stdout from the test.
  </action>
  <verify>
    <automated>npx playwright test e2e/pair-flow.spec.js --reporter=list 2>&amp;1 | tail -30; echo "expected: exit non-zero — /pair page doesn't exist yet, redirects don't fire, WS subprotocol gate not present"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f e2e/pair-flow.spec.js`
    - Contains the 3 expected test cases: `grep -c "^test(\|test\\.describe\\|test\\.beforeAll\\|test('" e2e/pair-flow.spec.js` returns at least 3
    - References the bootstrap OTP file: `grep -F 'bootstrap.otp' e2e/pair-flow.spec.js`
    - References localStorage.clideck.deviceToken: `grep -F 'clideck.deviceToken' e2e/pair-flow.spec.js`
    - `npx playwright test e2e/pair-flow.spec.js` exits non-zero (RED)
  </acceptance_criteria>
  <done>pair-flow.spec.js authored, RED, contracts the end-to-end pair UX for AC1+AC2+AC3+AC7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: Author e2e/revoke-flow.spec.js — Settings → Revoke → 4401 → /pair (RED)</name>
  <files>e2e/revoke-flow.spec.js</files>
  <read_first>
    - @e2e/session-pause.spec.js — installWsRecorder + injectMessageEvent pattern for driving the UI by feeding synthetic WS messages.
    - @e2e/smoke.spec.js — installWsRecorder reference.
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-CONTEXT.md D-06 — Revoke confirmation copy (exact strings to assert).
    - @.planning/2026-06-05-device-pairing-for-mobile-access/16-RESEARCH.md §10.6 — `device.revoke` and `device.list.get` arms.
  </read_first>
  <behavior>
    RED reason: there is no Settings → Linked devices panel, no device.list broadcast, no device.revoke message arm.

    Cases:
    - Pair (via the same bootstrap flow as pair-flow.spec.js) to land in the dashboard with a known token.
    - Open Settings → click the new "Linked devices" category button (`[data-cat="devices"]`). Assert the panel `#settings-devices` is visible (was hidden).
    - The panel renders a row for "This device" (matches state.deviceId).
    - Inject a `device.list` message that adds a second fake device (id='dev_other', label='Other Phone'). Assert a second row renders.
    - Click "Revoke" on the dev_other row → confirm modal opens with body text matching D-06 "other device" variant ("Other Phone will be signed out immediately…").
    - Confirm. Assert a `device.revoke` message was sent over WS with `{ deviceId: 'dev_other' }`.
    - Test 2 — self-revoke flow: click "Revoke" on the "This device" row → confirm modal opens with D-06 "this device" variant ("You'll be signed out of this browser immediately and the active session list will close…"). Confirm. Assert a `device.revoke` message is sent. Simulate the server response by closing the WS with code 4401 (via `await page.evaluate(() =&gt; window.__ws.close(4401, 'revoked'))` — Wave 2 plan 16-06's onclose handler should clear localStorage and navigate to /pair). Assert page navigates to /pair and localStorage.clideck.deviceToken is null.
  </behavior>
  <action>
    Create e2e/revoke-flow.spec.js. Header: Wave 0 RED for AC5, AC6, D-06.

    Reuse installWsRecorder. Add an `injectMessageEvent(page, msgObj)` helper (mirror e2e/session-pause.spec.js's approach): `await page.evaluate((data) =&gt; { window.__ws.dispatchEvent(new MessageEvent('message', { data: JSON.stringify(data) })); }, msgObj);`.

    For the modal-text assertions, locate `#cc-message` (confirm.js DOM target per @public/js/confirm.js line 2) and read textContent.

    Per CLAUDE.md §13: do not echo the captured token to test output.
  </action>
  <verify>
    <automated>npx playwright test e2e/revoke-flow.spec.js --reporter=list 2>&amp;1 | tail -30; echo "expected: exit non-zero — Settings panel + device.list / device.revoke arms not implemented yet"</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - Contains the two flow tests (other-device + self-revoke): `grep -c "test(" e2e/revoke-flow.spec.js` returns at least 2
    - References the D-06 copy: `grep -F 'will be signed out immediately' e2e/revoke-flow.spec.js`
    - References the 4401 close code: `grep -F '4401' e2e/revoke-flow.spec.js`
    - References the data-cat selector: `grep -F "data-cat=\"devices\"" e2e/revoke-flow.spec.js`
    - `npx playwright test e2e/revoke-flow.spec.js` exits non-zero (RED)
  </acceptance_criteria>
  <done>revoke-flow.spec.js authored, RED, contracts the full Settings → Revoke → 4401 → /pair UX.</done>
</task>

</tasks>

<verification>
After all 9 tasks land, the full suite still has its existing green baseline AND adds 7 new RED unit specs + 2 new RED e2e specs:

```bash
npx vitest run tests/pair-otp.test.js tests/devices-json.test.js tests/pair-redeem.test.js \
  tests/ws-auth-gate.test.js tests/revoke-closes-socket.test.js tests/bootstrap-otp.test.js \
  tests/device-revoke-rebuild.test.js
# Expected: exit 1, all 7 specs fail (the RED state we intend)

npx playwright test e2e/pair-flow.spec.js e2e/revoke-flow.spec.js
# Expected: exit non-zero (RED)

# Existing suite untouched
npx vitest run --reporter=verbose 2>&1 | tail -5
# Expected: pre-existing test count unchanged; 7 new "FAIL" entries appended.
```

Per CLAUDE.md §1, the RED state is the design intent — document it in the commit message and SUMMARY.md so a future reader doesn't mistake the failures for a regression. The plans 16-02 through 16-07 turn these specs green; plan 16-08 verifies and writes 16-VERIFICATION.md.

Cross-reference each test back to the Wave 1-3 plan that satisfies it:
- pair-otp.test.js → 16-03 (OTP layer)
- devices-json.test.js → 16-02 (devices.json persistence)
- pair-redeem.test.js → 16-04 (route handlers)
- ws-auth-gate.test.js → 16-05 (verifyClient gate)
- revoke-closes-socket.test.js → 16-05 (sessions.closeDevice) + 16-07 (handlers.js device.revoke arm)
- bootstrap-otp.test.js → 16-03 (bootstrapIfNeeded) + 16-04 (clearBootstrap on redeem)
- device-revoke-rebuild.test.js → 16-02 + 16-03 (the cycle integration)
- pair-flow.spec.js → 16-04 (HTTP routes) + 16-06 (client pair view + boot-time check)
- revoke-flow.spec.js → 16-07 (Settings panel) + 16-06 (onclose 4401 → /pair)

Every behaviour-adding task in Waves 1-3 has a RED test here. If a Wave 1-3 plan introduces behaviour not covered by one of these specs, that plan MUST also add an `it(...)` to the appropriate file before committing.
</verification>

<success_criteria>
- [ ] 7 vitest files exist under tests/ with `// @vitest-environment node` on line 1
- [ ] 2 Playwright spec files exist under e2e/
- [ ] `npx vitest run tests/pair-*.test.js tests/devices-json.test.js tests/ws-auth-gate.test.js tests/revoke-closes-socket.test.js tests/bootstrap-otp.test.js tests/device-revoke-rebuild.test.js` exits non-zero
- [ ] `npx playwright test e2e/pair-flow.spec.js e2e/revoke-flow.spec.js` exits non-zero
- [ ] Each test file's header comment documents the RED reason (which module/route/panel is missing)
- [ ] No raw tokens or real secrets in any test file (CLAUDE.md §13 — synthetic test strings only)
- [ ] No `~/.clideck/` pollution: every test uses CLIDECK_DATA_DIR=mkdtempSync(...) or playwright's TEST_HOME isolation
- [ ] Commit lands on `main`, NOT pushed (CLAUDE.md §3 — GitHub remote)
- [ ] 16-01-SUMMARY.md written documenting the RED contracts and which Wave 1-3 plans satisfy each
</success_criteria>

<output>
Create `.planning/2026-06-05-device-pairing-for-mobile-access/16-01-SUMMARY.md` with:

- Per-spec breakdown: file name, AC mapping, RED reason (the missing module/route/panel), and which Wave 1-3 plan turns it green.
- Quote the vitest + playwright failing-runs to prove RED state.
- Note the Phase 15 merge-order pressure from PATTERNS §3 — once Phase 15 lands, re-run the e2e specs to confirm the line shifts in handlers.js / server.js don't accidentally green any of these.
- Commit-message-style summary that a checker can quote into 16-VERIFICATION.md.
</output>
