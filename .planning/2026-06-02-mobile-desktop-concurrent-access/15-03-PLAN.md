---
phase: 15-mobile-desktop-concurrent-access
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - handlers.js
autonomous: true
requirements: [R1, R5]
must_haves:
  truths:
    - "Server no longer exposes any `case 'remote.*':` WS message arms"
    - "Server broadcasts `{type:'clients.count', count: N}` when a client connects"
    - "Server broadcasts `{type:'clients.count', count: N}` when a client disconnects"
    - "New client receives a `clients.count` message during its initial-payload window"
    - "`compareVersions`, `parseVersion`, `getInstalledVersion` are preserved (still used by checkAvailability)"
    - "Server starts without errors (`node server.js` boots cleanly)"
  artifacts:
    - path: "handlers.js"
      provides: "R1 server-side deletion sweep + R5 server-side broadcast wiring"
      contains: "clients.count"
  key_links:
    - from: "handlers.js onConnection (after sessions.clients.add)"
      to: "sessions.broadcast"
      via: "broadcast({type:'clients.count', count: sessions.clients.size})"
      pattern: "clients\\.count.*sessions\\.clients\\.size"
    - from: "handlers.js ws.on('close')"
      to: "sessions.broadcast"
      via: "after sessions.clients.delete(ws), broadcast new count"
      pattern: "clients\\.delete.*clients\\.count|clients\\.count.*delete"
---

<objective>
Implement the server-side half of R1 (clideck-remote retirement) and R5 (other-client indicator broadcast). This plan touches ONE file — `handlers.js` — and deletes ~120 lines of remote bridges + the `remoteUpdateCache` / `checkRemoteUpdate` / `remoteCliEnv` helpers, then adds two `sessions.broadcast({type:'clients.count', count: sessions.clients.size})` calls in `onConnection`.

Purpose: Remove the server's `clideck-remote` plumbing entirely (per D-01, D-02, D-03) and introduce the server-wide presence broadcast (per D-08, D-09, D-11).

Output: handlers.js diff that flips `e2e/clideck-remote-deletion.spec.js` partially green (server half) and unblocks the R4/R5 E2E specs that need the broadcast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md

@handlers.js
@sessions.js

<interfaces>
<!-- Key contracts. Extracted from PATTERNS.md §1 + RESEARCH.md §1a + §2. -->

From sessions.js (unchanged — DO NOT modify in this plan):
```javascript
// sessions.js — exported (used by handlers.js)
function broadcast(msg) {
  const raw = JSON.stringify(msg);
  for (const c of clients) {
    if (c.readyState !== 1) continue;  // skips disconnecting clients automatically
    c.send(raw);
  }
  for (const listener of broadcastListeners) { try { listener(msg); } catch {} }
}
const clients = new Set();  // populated by handlers.js
```

From handlers.js (current — lines to delete and add to):
- Lines 46-98: `remoteUpdateCache`, `remoteUpdateCheckedAt`, `REMOTE_UPDATE_INTERVAL`, `compareVersions`, `parseVersion`, `getInstalledVersion`, `checkRemoteUpdate(ws)` — DELETE `remoteUpdateCache`, `remoteUpdateCheckedAt`, `REMOTE_UPDATE_INTERVAL`, `checkRemoteUpdate`; KEEP `compareVersions`, `parseVersion`, `getInstalledVersion` (used by `checkAvailability` at line ~102).
- Line 246-248: `function remoteCliEnv()` — DELETE (no surviving callers).
- Line 251: `sessions.clients.add(ws);` — ADD broadcast immediately after.
- Line 272-280: Initial-payload block (config, themes, presets, sessions, sessions.resumable, transcript.cache, plugins, pills, sendBuffers). DO NOT add `clients.count` here — the broadcast at line 252 already covers the new client.
- Line 355: `case 'resize': sessions.resize(msg); break;` — DO NOT touch (Plan 02 made sessions.resize a no-op).
- Lines 601-650: Five `case 'remote.*':` arms — DELETE.
- Line 658: `ws.on('close', () => sessions.clients.delete(ws));` — EXTEND to also broadcast.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Delete clideck-remote bridges in handlers.js + add clients.count broadcasts</name>
  <files>handlers.js</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md (R1 + R5)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-01, D-02, D-03 for deletion; D-08, D-09, D-11 for broadcast)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§1a verified deletion inventory + §2 sessions.clients lifecycle + §10 G1 G6 G7 G8 risks + "Code Examples → Server: D-09 clients.count broadcast")
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§1 — initial-payload precedent, close-handler extension precedent, deletion analog)
    - handlers.js (read full file — verify exact line numbers; RESEARCH.md is pinned to commit 9f6a111 so line drift is unlikely but possible)
    - sessions.js (verify `sessions.broadcast` signature + that `sessions.clients` is exported)
    - e2e/clideck-remote-deletion.spec.js (Wave 0 — verify the grep gate this task makes green for server-side files)
  </read_first>
  <behavior>
    Deletions (R1 — D-02, verified by 15-RESEARCH.md §1a):
    - Delete `remoteUpdateCache`, `remoteUpdateCheckedAt`, `REMOTE_UPDATE_INTERVAL` constants (lines 46-49 area).
    - Delete `checkRemoteUpdate(ws)` function (lines 73-98 area).
    - KEEP `compareVersions`, `parseVersion`, `getInstalledVersion` (still used by `checkAvailability` at line ~102 — per G6).
    - Delete `function remoteCliEnv()` at lines 246-248 area.
    - Delete `case 'remote.status':` (lines 601-612 area).
    - Delete `case 'remote.pair':` (lines 614-621 area).
    - Delete `case 'remote.unpair':` (lines 623-632 area).
    - Delete `case 'remote.getHistory':` (lines 634-637 area).
    - Delete `case 'remote.install':` (lines 639-650 area).

    Additions (R5 — D-09):
    - In `onConnection(ws)`, immediately after the existing `sessions.clients.add(ws);` (line ~251), add:
        `sessions.broadcast({ type: 'clients.count', count: sessions.clients.size });`
      This single broadcast fans out to ALL clients including the just-added one (per `sessions.broadcast` iterating `clients` set with the readyState=1 filter). Per RESEARCH.md G8, this is the single source of truth — DO NOT also add a per-client `ws.send` in the initial-payload block.
    - Extend the existing `ws.on('close', () => sessions.clients.delete(ws));` at line ~658 to:
        `ws.on('close', () => { sessions.clients.delete(ws); sessions.broadcast({ type: 'clients.count', count: sessions.clients.size }); });`
      The broadcast fires AFTER the delete, so the remaining clients see the correct decremented count.

    Preservation:
    - Do NOT modify the `case 'resize':` arm at line ~355 (Plan 02 handled the server-side no-op).
    - Do NOT modify the initial-payload sends (config/themes/presets/sessions/sessions.resumable/transcript.cache/plugins/pills) at lines ~272-280 — broadcast at line 252 already reaches the new client.
    - Do NOT modify the heartbeat `ws.on('close', () => clearInterval(heartbeat))` at line ~270 — both close handlers fire in registration order (per RESEARCH.md G7). Only extend the line-658 handler.
    - KEEP `compareVersions`, `parseVersion`, `getInstalledVersion` — these are called by `checkAvailability` (line ~102/110/111) and have non-remote consumers.
  </behavior>
  <action>
    Read handlers.js in full first (Read tool, single pass; expect ~700 lines). Confirm the line numbers from 15-RESEARCH.md §1a against the actual file. If line drift is detected (commit 9f6a111 vs current HEAD), grep for the specific identifiers to locate the actual targets:
    - `grep -n "remoteUpdateCache\\|REMOTE_UPDATE_INTERVAL\\|checkRemoteUpdate" handlers.js`
    - `grep -n "remoteCliEnv" handlers.js`
    - `grep -n "case 'remote\\." handlers.js`
    - `grep -n "sessions.clients.add\\|sessions.clients.delete" handlers.js`

    Use Edit operations to perform each deletion as a separate logical chunk:

    Edit 1 — Delete `remoteUpdateCache`, `remoteUpdateCheckedAt`, `REMOTE_UPDATE_INTERVAL` declarations. Per RESEARCH.md §1a these live around lines 46-49 (verify exact). Confirm with `grep -nB1 -A1 "remoteUpdateCache" handlers.js` first. The deletion target is the variable declarations + any documenting comment block. KEEP `compareVersions`, `parseVersion`, `getInstalledVersion` function declarations adjacent.

    Edit 2 — Delete the `checkRemoteUpdate(ws)` function entirely (RESEARCH.md §1a lines 73-98 area). Verify body with `grep -nA 30 "function checkRemoteUpdate" handlers.js` before deleting.

    Edit 3 — Delete `function remoteCliEnv()` (RESEARCH.md §1a lines 246-248 area). Confirm with `grep -nA 3 "function remoteCliEnv" handlers.js`.

    Edit 4 — Add the `clients.count` broadcast immediately after `sessions.clients.add(ws);` in `onConnection`. Per PATTERNS.md §1a + RESEARCH.md "Code Examples → Server: D-09 clients.count broadcast", the inserted line is:
        `  sessions.broadcast({ type: 'clients.count', count: sessions.clients.size });`
    Place it on the line directly after `sessions.clients.add(ws);` with matching indentation.

    Edit 5 — Delete the 5 `case 'remote.*':` arms (RESEARCH.md §1a lines 601-650). Per the inventory there are FIVE arms: `case 'remote.status':`, `case 'remote.pair':`, `case 'remote.unpair':`, `case 'remote.getHistory':`, `case 'remote.install':`. Delete each block from its `case 'remote.X':` line through its closing `break;` (or closing `}` for block-style cases). The surrounding switch structure stays intact.

    Edit 6 — Extend the close handler at line ~658. Before:
        `ws.on('close', () => sessions.clients.delete(ws));`
    After:
        `ws.on('close', () => { sessions.clients.delete(ws); sessions.broadcast({ type: 'clients.count', count: sessions.clients.size }); });`

    After all edits, run `node --check handlers.js` to catch syntax errors. Run `node -e "const h = require('./handlers.js'); console.log(Object.keys(h))"` to verify the module loads.

    Boot the server and verify it starts cleanly: `PORT=4099 CLIDECK_DATA_DIR=$(mktemp -d) timeout 5 node server.js 2>&1 | head -40` should print the listening-on message without error stacks. Kill via the timeout.

    Run the verification grep from D-03 / RESEARCH.md §1h — note that it will still find matches in `public/index.html`, `public/js/app.js`, `public/js/settings.js`, `public/js/state.js` (those are Plan 04's territory). The server-side grep specifically should be clean:
        `grep -nE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate" handlers.js`
    This MUST return zero matches.

    Run the existing test suite to verify no regression: `npx vitest run`. The expected outcomes:
    - tests/sessions-resize.test.js stays GREEN (Plan 02 made it green).
    - All other existing tests stay GREEN.
    - tests/other-client-indicator.test.js stays RED (Plan 05 is what makes it green — that requires the updateOtherClientIndicator export which doesn't exist yet).
  </action>
  <verify>
    <automated>set -e; node --check handlers.js && grep -cE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate" handlers.js | grep -v '^#' || true; SERVER_GREP=$(grep -cE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate" handlers.js || echo 0); echo "Server grep matches: $SERVER_GREP (expect 0)"; test "$SERVER_GREP" = "0"; CC_BROADCAST=$(grep -cE "clients\\.count.*sessions\\.clients\\.size|clients\\.size.*clients\\.count" handlers.js); echo "clients.count broadcast count: $CC_BROADCAST (expect ≥ 2)"; test "$CC_BROADCAST" -ge 2; npx vitest run 2>&1 | tee /tmp/12-03-vitest.log</automated>
  </verify>
  <acceptance_criteria>
    - `node --check handlers.js` exits 0 (no syntax errors)
    - `grep -c "case 'remote\\." handlers.js` returns 0 (all 5 remote case arms deleted)
    - `grep -c "function remoteCliEnv" handlers.js` returns 0 (helper deleted)
    - `grep -c "remoteUpdateCache\\|REMOTE_UPDATE_INTERVAL\\|checkRemoteUpdate" handlers.js` returns 0 (cache + check function deleted)
    - `grep -c "function compareVersions\\|function parseVersion\\|function getInstalledVersion" handlers.js` returns 3 (these are PRESERVED)
    - `grep -cE "sessions\\.broadcast\\(.{0,40}type:.{0,5}.clients\\.count" handlers.js` ≥ 2 (broadcast appears on connect AND on close)
    - One broadcast call is on a line immediately after `sessions.clients.add(ws);` (verify: `grep -nA 1 "sessions.clients.add" handlers.js` shows clients.count broadcast on the next line)
    - One broadcast call is in the same callback as `sessions.clients.delete(ws)` (verify: `grep -nA 1 "sessions.clients.delete" handlers.js` shows clients.count broadcast on the next line OR same line)
    - Server-side R1 grep (the verbose pattern from D-03 limited to handlers.js) returns 0 matches
    - `npm run test` exits 0 — full vitest unit suite passes (tests/sessions-resize.test.js green from Plan 02; tests/display-sizing.test.js green; tests/other-client-indicator.test.js still RED because Plan 05 hasn't landed — this is expected)
    - Booting `node server.js` for ≤5 seconds produces no error stack (timeout-bounded smoke test)
    - Verbose commit message references R1 + R5 + D-01..D-03 + D-09 per CLAUDE.md §5
  </acceptance_criteria>
  <done>handlers.js has all clideck-remote bridges removed and the clients.count broadcast wired on connect + close; server boots clean; vitest unit suite passes.</done>
</task>

</tasks>

<verification>
- `grep -nE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate" handlers.js` → zero matches
- `grep -nE "sessions\\.broadcast.{0,40}clients\\.count" handlers.js` → 2 matches (connect + close)
- `node --check handlers.js` → exit 0
- `npm run test` → exit 0 (unit suite passes; remote-deletion E2E grep covers other files in Plan 04)
- Boot smoke: `PORT=4099 CLIDECK_DATA_DIR=$(mktemp -d) timeout 5 node server.js | head -40` shows clean startup
</verification>

<success_criteria>
Plan 03 is complete when:
- [ ] All 5 server-side `case 'remote.*':` arms deleted from handlers.js
- [ ] `remoteUpdateCache`, `remoteUpdateCheckedAt`, `REMOTE_UPDATE_INTERVAL`, `checkRemoteUpdate`, `remoteCliEnv` deleted
- [ ] `compareVersions`, `parseVersion`, `getInstalledVersion` PRESERVED (per G6)
- [ ] `clients.count` broadcast added immediately after `sessions.clients.add(ws)` in `onConnection`
- [ ] `clients.count` broadcast added inside the close handler at line ~658 (after `sessions.clients.delete(ws)`)
- [ ] Server-side R1 grep returns zero
- [ ] `npm run test` passes (no regression)
- [ ] Server boots clean
- [ ] Commit message verbose per CLAUDE.md §5
</success_criteria>

<output>
Create `.planning/2026-06-02-mobile-desktop-concurrent-access/15-03-SUMMARY.md` when done — record the exact line ranges deleted, the two broadcast call sites added, and confirm the server-side R1 grep is clean. Note that the client-side R1 deletion sweep is Plan 04's responsibility.
</output>
