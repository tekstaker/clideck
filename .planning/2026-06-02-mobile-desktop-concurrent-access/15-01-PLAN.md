---
phase: 15-mobile-desktop-concurrent-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/sessions-resize.test.js
  - tests/other-client-indicator.test.js
  - e2e/clideck-remote-deletion.spec.js
  - e2e/pty-size-locked.spec.js
  - e2e/mobile-touch.spec.js
  - e2e/concurrent-input.spec.js
  - e2e/mobile-viewport.spec.js
  - e2e/session-indicator-mutex.spec.js
autonomous: true
requirements: [R1, R2, R3, R4, R5, R6]
must_haves:
  truths:
    - "All 7 new test/spec files exist on disk"
    - "Running `npm run test` produces RED on the two new vitest files (will turn green once Plans 02-05 land)"
    - "Running `npm run test:e2e` (when Chromium libs present) produces RED on the 5 new Playwright specs"
    - "tests/display-sizing.test.js still passes (no regression to the existing resize-sending assertion)"
    - "e2e/session-indicator-mutex.spec.js extended assertion about .other-client-indicator slot independence exists in the file"
  artifacts:
    - path: "tests/sessions-resize.test.js"
      provides: "R2 unit gate — server resize no-op assertion"
      contains: "sessions.resize"
    - path: "tests/other-client-indicator.test.js"
      provides: "R5 unit gate — DOM toggle + G9 newly-added-row coverage"
      contains: "updateOtherClientIndicator"
    - path: "e2e/clideck-remote-deletion.spec.js"
      provides: "R1 E2E gate — DOM absence + console-error gate"
      contains: "#remote-modal"
    - path: "e2e/pty-size-locked.spec.js"
      provides: "R2 E2E gate — send resize, assert term.cols unchanged"
      contains: "type: 'resize'"
    - path: "e2e/mobile-touch.spec.js"
      provides: "R3 E2E gate — iPhone 12 context tap → xterm-helper-textarea focus"
      contains: "xterm-helper-textarea"
    - path: "e2e/concurrent-input.spec.js"
      provides: "R4 + R5 E2E gate — two-context concurrent input + indicator visibility"
      contains: "browser.newContext"
    - path: "e2e/mobile-viewport.spec.js"
      provides: "R6 E2E gate — no horizontal page-body overflow at 375×667 + walkthrough"
      contains: "scrollWidth"
  key_links:
    - from: "tests/sessions-resize.test.js"
      to: "sessions.resize (to be no-op'd in Plan 02)"
      via: "vitest expect(pty.resize).not.toHaveBeenCalled()"
      pattern: "expect.*pty.resize.*not.*toHaveBeenCalled"
    - from: "tests/other-client-indicator.test.js"
      to: "public/js/terminals.js updateOtherClientIndicator export (to be added in Plan 05)"
      via: "import { updateOtherClientIndicator } from '../public/js/terminals.js'"
      pattern: "updateOtherClientIndicator"
    - from: "e2e/clideck-remote-deletion.spec.js"
      to: "public/index.html (deletions in Plan 04)"
      via: "page.locator('#btn-remote').toHaveCount(0)"
      pattern: "toHaveCount\\(0\\)"
    - from: "e2e/concurrent-input.spec.js"
      to: "handlers.js clients.count broadcast (Plan 03) + state.otherClientsConnected toggle (Plan 05)"
      via: ".other-client-indicator class poll"
      pattern: "other-client-indicator"
---

<objective>
Author all 8 Wave 0 test files (2 vitest + 6 Playwright) BEFORE any implementation lands. Per CLAUDE.md §2 these tests are the DEFINITION OF DONE for Plans 02–05 — they MUST be authored to fail today and turn green only after the corresponding implementation tasks complete. This plan is purely test creation; no production code is touched.

Purpose: TDD-first per CLAUDE.md §2 + Phase 12's Validation Architecture (15-VALIDATION.md). The tests are the contract; downstream plans implement to flip them green.

Output: 7 new test/spec files + 1 extended existing spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-VALIDATION.md

@tests/session-pause.test.js
@tests/terminal-focus.test.js
@tests/display-sizing.test.js
@e2e/smoke.spec.js
@e2e/session-indicator-mutex.spec.js
@playwright.config.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Author 2 vitest unit tests (R2 + R5)</name>
  <files>tests/sessions-resize.test.js, tests/other-client-indicator.test.js</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md (R2 + R5 acceptance criteria)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-04, D-08, D-09, D-10, D-11)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md ("Other-client indicator — exact contract" section)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§9 + §10 + Shared 5 + Shared 6)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§9 Validation Architecture)
    - tests/session-pause.test.js (node-env analog — copy freshSessionsModule / captureClient shape)
    - tests/terminal-focus.test.js (happy-dom analog — copy import + beforeEach shape)
    - sessions.js (verify current resize body at line 368 and exports list at ~line 749 before mocking)
    - public/js/state.js (verify current state literal shape; the test imports state and toggles otherClientsConnected)
    - public/js/terminals.js (verify there is currently NO updateOtherClientIndicator export — test must fail until Plan 05 adds it)
  </read_first>
  <behavior>
    tests/sessions-resize.test.js (R2):
    - Test 1: calling `sessions.resize({id, cols: 40, rows: 10})` against a session whose pty has a `vi.fn()` spy does NOT invoke the spy
    - Test 2: calling `sessions.resize({id: 'ghost', cols: 80, rows: 24})` for an unknown id does not throw
    - Test 3: calling `sessions.resize({})` (no id) does not throw

    tests/other-client-indicator.test.js (R5 + G9 risk):
    - Test 1: with two `.other-client-indicator.hidden` spans in document.body, `updateOtherClientIndicator(2)` removes `.hidden` from both and sets `state.otherClientsConnected = true`
    - Test 2: with two `.other-client-indicator` spans (no `.hidden`), `updateOtherClientIndicator(1)` re-adds `.hidden` to both and sets `state.otherClientsConnected = false`
    - Test 3 (G9 — newly-added row regression): when `state.otherClientsConnected = true`, calling `updateOtherClientIndicator(2)` then injecting a NEW `.other-client-indicator.hidden` span into the DOM and re-calling `updateOtherClientIndicator(2)` results in the new span no longer carrying `.hidden`
    - Test 4: `updateOtherClientIndicator(2)` is a no-op when no indicator spans exist (no throw)
  </behavior>
  <action>
    Create `tests/sessions-resize.test.js`:
    - Use `// @vitest-environment node` directive on line 1 per PATTERNS.md §9a / Shared 5.
    - Import shape: `import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';` and the tmpdir/path/fs imports as used by `tests/session-pause.test.js`.
    - Copy the `freshSessionsModule()` helper VERBATIM from `tests/session-pause.test.js` lines ~24-32 (require-cache wipe scoped to non-node_modules clideck paths).
    - Copy the `beforeEach` / `afterEach` `CLIDECK_DATA_DIR` tmpdir setup/teardown VERBATIM from session-pause.test.js lines ~38-46.
    - Define a minimal `fakeLiveSession({id})` that returns `{ name: 'A', sessionToken: null, pty: { resize: vi.fn() } }` per PATTERNS.md §9b (spy target is `pty.resize`, NOT `pty.kill`).
    - In each test, do `const sessions = freshSessionsModule(); const map = sessions.getSessions(); map.set('sess-A', fakeLiveSession({id:'sess-A'}));` then call `sessions.resize(...)` and assert with `expect(s.pty.resize).not.toHaveBeenCalled()`.
    - The describe block label: `'sessions.resize — locked at session creation (Phase 12 R2)'`.
    - This test will FAIL today because `sessions.js:368` currently reads `function resize(msg) { sessions.get(msg.id)?.pty.resize(msg.cols, msg.rows); }` — Plan 02 makes it pass.

    Create `tests/other-client-indicator.test.js`:
    - Use `// @vitest-environment happy-dom` directive on line 1 per PATTERNS.md §10a / Shared 6.
    - Import shape: `import { describe, it, expect, beforeEach } from 'vitest';` + `import { state } from '../public/js/state.js';` + `import { updateOtherClientIndicator } from '../public/js/terminals.js';`.
    - The import of `updateOtherClientIndicator` will FAIL today because the export does not exist — Plan 05 adds it. Vitest reports this as a load failure, which is the expected RED state.
    - `beforeEach`: `state.otherClientsConnected = false; document.body.innerHTML = '';`.
    - Test 1 markup template literal (from PATTERNS.md §10b):
        `<span class="other-client-indicator hidden"></span><span class="other-client-indicator hidden"></span>`
    - Test 2 markup: two spans WITHOUT `hidden`, set `state.otherClientsConnected = true` first to simulate already-on.
    - Test 3 (G9): set up with `state.otherClientsConnected = true` + one span without `hidden`, call updater(2), then `document.body.insertAdjacentHTML('beforeend', '<span class="other-client-indicator hidden"></span>')`, call updater(2) AGAIN, assert the new span has `.hidden` removed.
    - The describe block label: `'updateOtherClientIndicator (Phase 12 R5)'`.
  </action>
  <verify>
    <automated>npx vitest run tests/sessions-resize.test.js tests/other-client-indicator.test.js 2>&1 | tee /tmp/12-01-task1-vitest.log; echo "Expected: both files exist and FAIL (sessions-resize test fails because pty.resize IS called; other-client-indicator fails because import resolves to undefined). Exit code 1 expected."</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/sessions-resize.test.js` exists and contains the string `// @vitest-environment node`
    - File `tests/sessions-resize.test.js` contains `expect(s.pty.resize).not.toHaveBeenCalled()` (or equivalent `not.toHaveBeenCalled()` assertion on the resize spy)
    - File `tests/sessions-resize.test.js` contains the describe label `sessions.resize — locked at session creation (Phase 12 R2)`
    - File `tests/other-client-indicator.test.js` exists and contains `// @vitest-environment happy-dom`
    - File `tests/other-client-indicator.test.js` contains `import { updateOtherClientIndicator } from '../public/js/terminals.js'`
    - File `tests/other-client-indicator.test.js` contains at least 3 `it(` test cases including one labelled (case-insensitive) "newly-added" or "G9"
    - Running `npx vitest run tests/sessions-resize.test.js tests/other-client-indicator.test.js` exits non-zero (RED) and the failure reason is the missing export / pty.resize being called — NOT a syntax / import-resolution typo
    - Running `npx vitest run tests/display-sizing.test.js` still exits 0 (no regression)
  </acceptance_criteria>
  <done>Both vitest files committed, fail with the expected red-state reasons documented above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Author 5 new Playwright specs + extend session-indicator-mutex.spec.js</name>
  <files>e2e/clideck-remote-deletion.spec.js, e2e/pty-size-locked.spec.js, e2e/mobile-touch.spec.js, e2e/concurrent-input.spec.js, e2e/mobile-viewport.spec.js, e2e/session-indicator-mutex.spec.js</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md (R1, R2, R3, R4, R5, R6 acceptance criteria)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-08 server-wide count, D-13 D-15 touch, D-16 responsive, D-17 D-18 testing)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md ("Other-client indicator — exact contract", "Phone-viewport responsive contract", "Deletion contract")
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§6 mobile-context pattern, §7 two-context pattern, §8 soft-keyboard verification)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§11, §12, §13, §14, §15 + Shared 7 + Shared 8)
    - e2e/smoke.spec.js (analog for installWsRecorder, waitForAppReady, error-listener pattern — copy verbatim)
    - e2e/session-indicator-mutex.spec.js (analog for spawnSession, WS send idiom — to be EXTENDED in this task too)
    - playwright.config.js (verify port 4099, workers: 1, fullyParallel: false — two-context tests rely on shared server)
    - public/js/terminals.js (verify `.term-wrap` selector exists at line ~537 — referenced in mobile-touch + mobile-viewport)
    - public/index.html (verify `#mobile-nav-toggle` + `#mobile-nav-close` selectors exist — referenced in mobile-viewport walkthrough)
  </read_first>
  <behavior>
    e2e/clideck-remote-deletion.spec.js (R1):
    - Test 1: After page load, `#btn-remote` is absent (toHaveCount 0)
    - Test 2: `#remote-modal` is absent (toHaveCount 0)
    - Test 3: `#version-remote` is absent (toHaveCount 0)
    - Test 4: No pageerror / console.error during page load

    e2e/pty-size-locked.spec.js (R2):
    - Test 1: Create a session sized 120×30, send a hand-crafted `{type:'resize', id, cols:40, rows:10}` over the recorded WS, poll for ≥1s, assert `term.cols === 120` (PTY size unchanged)

    e2e/mobile-touch.spec.js (R3 / D-13 verification):
    - Test 1: In iPhone 12 mobile context, tap `.term-wrap` after spawning a session, assert `document.activeElement` has class `xterm-helper-textarea`

    e2e/concurrent-input.spec.js (R4 + R5):
    - Test 1 (R4): Two browser contexts attach to one session; A sends `echo A\r`, B sends `echo B\r`; both contexts observe both `A` and `B` in their output stream
    - Test 2 (R5): Tab A loads, `.other-client-indicator` has class `hidden`; Tab B opens → indicator loses `hidden` on A within 5s; Tab B closes → indicator regains `hidden` on A within 10s

    e2e/mobile-viewport.spec.js (R6):
    - Test 1: In iPhone 12 mobile context, `document.body.scrollWidth === window.innerWidth` (no page-body overflow); `#mobile-nav-toggle` visible
    - Test 2 (walkthrough): create session → tap `#mobile-nav-toggle` → expect `body.mobile-nav-open` → tap `#mobile-nav-close` → expect `body.mobile-nav-open` absent → re-assert no horizontal page overflow

    e2e/session-indicator-mutex.spec.js (extend):
    - New assertion: after setting `state.otherClientsConnected = true` via the helper (or after Tab B connects), the `.other-client-indicator` lives in the top row (sibling of `.session-time`), NOT colliding with `.unread-dot` or `.session-status` (different DOM ancestor div within the row). Selector independence proof: `.other-client-indicator` is NOT a descendant of the same `<div>` that hosts `.unread-dot`.
  </behavior>
  <action>
    Create the 5 new spec files using the exact shapes from 15-PATTERNS.md §11–§15. All five MUST inline-copy these helpers verbatim from e2e/smoke.spec.js (per PATTERNS.md Shared 7 — no shared helpers file exists yet, and creating one is out of scope for this plan):
    - `installWsRecorder(page)` (smoke.spec.js lines ~14-43) — but ALSO record received messages as `w.__rxMessages` (array of parsed objects) per session-indicator-mutex.spec.js's recorder, because R2/R4/R5 specs need to filter by `m.type === 'output'` / `m.type === 'created'`. Use the richer recorder from session-indicator-mutex.spec.js as canonical.
    - `waitForAppReady(page)` (smoke.spec.js lines ~45-53)
    - `spawnSession(page, opts = { cols: 80, rows: 24 })` (session-indicator-mutex.spec.js lines ~69-88) — extended to accept cols/rows parameters for pty-size-locked.spec.js's 120×30 case.

    File 1 — `e2e/clideck-remote-deletion.spec.js`:
    - Copy bootstrap from PATTERNS.md §11c. Single test that registers `page.on('pageerror', ...)` + `page.on('console', msg => msg.type() === 'error' && ...)`, navigates to '/', awaits waitForAppReady, then asserts the three `toHaveCount(0)` checks and `expect(errors).toEqual([])`.
    - Optionally add a second `test('R1 grep — repo contains no clideck-remote refs outside CHANGELOG/.planning', async () => { ... })` block that uses `child_process.execSync` to run the verification grep from 15-CONTEXT.md D-03 / 15-RESEARCH.md §1h. If matches > 0, fail with the grep output. This makes the grep gate automated.

    File 2 — `e2e/pty-size-locked.spec.js`:
    - Bootstrap helpers verbatim. Call `spawnSession(page, { cols: 120, rows: 30 })`. After session created, `page.evaluate` to send `{type:'resize', id: sessionId, cols: 40, rows: 10}` over `w.__ws`. Then `expect.poll` reading `state.terms.get(id)?.term.cols` for up to 2 seconds, asserting it stays `=== 120`. Per PATTERNS.md §12c the dynamic `import('/js/state.js')` may need an alternative — fallback is to read `entry.term.cols` via `state.terms` exposed by querying the DOM grid columns from xterm internals.
    - As a backup assertion, also send the resize and poll `state.terms.get(id)?.term.rows === 30`.

    File 3 — `e2e/mobile-touch.spec.js`:
    - Use `const { test, expect, devices } = require('@playwright/test');`. Inside the test body: `const ctx = await browser.newContext({ ...devices['iPhone 12'] }); const page = await ctx.newPage();` then installWsRecorder + waitForAppReady + spawnSession (helpers inlined at top of file).
    - After spawnSession: `await page.locator(\`.group[data-id="${id}"]\`).tap();` then `await page.locator('.term-wrap').first().tap();`. Then `expect(await page.evaluate(() => document.activeElement?.classList?.contains('xterm-helper-textarea'))).toBe(true);`. Close ctx.

    File 4 — `e2e/concurrent-input.spec.js`:
    - Two tests sharing helpers at top.
    - Test 1 (R4): Per PATTERNS.md §14b, two `browser.newContext()` calls, each newPage + installWsRecorder + goto + waitForAppReady. A spawns session, B awaits the same row visible. Both tabs click the session. Both tabs `page.evaluate` send `{type:'input', id, data: 'echo A\r'}` / `'echo B\r'`. `expect.poll` on both pages asserts `m.type === 'output'` data contains both `A` and `B`.
    - Test 2 (R5 indicator visibility): Per PATTERNS.md §14c. A loads alone, spawn session, assert `.group[data-id="${id}"] .other-client-indicator` has class `hidden`. B connects (new context + new page + waitForAppReady). On A, `expect(locator).not.toHaveClass(/\\bhidden\\b/, { timeout: 5_000 })`. Then `ctxB.close()`. On A, `expect(locator).toHaveClass(/\\bhidden\\b/, { timeout: 10_000 })`.

    File 5 — `e2e/mobile-viewport.spec.js`:
    - Per PATTERNS.md §15a + §15b. Test 1: iPhone 12 context, assert `document.body.scrollWidth > window.innerWidth` is false; `#mobile-nav-toggle` visible. Test 2: walkthrough — spawnSession, `#mobile-nav-toggle.tap()`, expect `body.mobile-nav-open` count 1; `#mobile-nav-close.tap()`, expect count 0; re-assert no overflow.

    File 6 — extend `e2e/session-indicator-mutex.spec.js`:
    - Read the existing file's test structure first. Append a new `test('R5 indicator does NOT collide with unread-dot or session-status slots', ...)` block at the end. Inside: spawn a session, simulate `state.otherClientsConnected = true` via `page.evaluate(() => window.__updateOther && window.__updateOther(2))` — actually, since the helper isn't globally exposed, instead synthesize the WS message: `await page.evaluate(({id}) => w.__ws.dispatchEvent(new MessageEvent('message', { data: JSON.stringify({ type: 'clients.count', count: 2 }) })), ...)` is not possible because the WebSocket is not user-dispatchable; INSTEAD, the simpler approach: set `state.otherClientsConnected = true` directly via `page.evaluate(async () => { const { state } = await import('/js/state.js'); state.otherClientsConnected = true; document.querySelectorAll('.other-client-indicator').forEach(el => el.classList.remove('hidden')); })`. Then assert the row's `.other-client-indicator` is a sibling of `.session-time` (inside the `.flex.items-baseline.gap-2` parent), and is NOT a descendant of the `.flex.items-center.gap-1.mt-0.5` parent (which holds `.unread-dot` and `.session-status`).
    - Concrete assertion: `await expect(page.locator(\`.group[data-id="${id}"] > div > div:first-child .other-client-indicator\`)).toHaveCount(1)` (top row contains it) and `await expect(page.locator(\`.group[data-id="${id}"] > div > div:last-child .other-client-indicator\`)).toHaveCount(0)` (bottom row does not). Adjust selectors based on the actual row DOM structure from terminals.js:507-526.
  </action>
  <verify>
    <automated>ls -1 e2e/clideck-remote-deletion.spec.js e2e/pty-size-locked.spec.js e2e/mobile-touch.spec.js e2e/concurrent-input.spec.js e2e/mobile-viewport.spec.js && grep -l "other-client-indicator" e2e/session-indicator-mutex.spec.js && echo "All 6 spec files present, mutex spec extended"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new files exist: `e2e/clideck-remote-deletion.spec.js`, `e2e/pty-size-locked.spec.js`, `e2e/mobile-touch.spec.js`, `e2e/concurrent-input.spec.js`, `e2e/mobile-viewport.spec.js`
    - Each new file contains `require('@playwright/test')` import
    - `e2e/clideck-remote-deletion.spec.js` contains `#btn-remote`, `#remote-modal`, `#version-remote`, and `toHaveCount(0)`
    - `e2e/pty-size-locked.spec.js` contains `type: 'resize'` AND `cols: 120` (the create) AND `cols: 40` (the malicious resize)
    - `e2e/mobile-touch.spec.js` contains `devices['iPhone 12']` AND `xterm-helper-textarea`
    - `e2e/concurrent-input.spec.js` contains TWO `browser.newContext()` calls AND `other-client-indicator`
    - `e2e/mobile-viewport.spec.js` contains `devices['iPhone 12']` AND `scrollWidth` AND `#mobile-nav-toggle`
    - `e2e/session-indicator-mutex.spec.js` (existing file) now contains the literal `other-client-indicator` somewhere
    - Each new spec file inlines `installWsRecorder` AND `waitForAppReady` AND `spawnSession` helpers (verify by `grep -c installWsRecorder` returning ≥1 per file, excluding the mutex spec which already had them)
    - `grep -v '^//' e2e/clideck-remote-deletion.spec.js | grep -v '^#' | grep -c "test(" ` is ≥ 1 (at least one test block)
    - Syntax check: `node --check e2e/clideck-remote-deletion.spec.js` succeeds for all 5 new files (no Playwright runtime required for syntax check)
  </acceptance_criteria>
  <done>All 6 spec files committed with the expected helper inlining and assertion patterns. Per CLAUDE.md §1, do NOT claim "they all run green" — the design intent is that they fail today; document this in the commit message.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `ls tests/sessions-resize.test.js tests/other-client-indicator.test.js` → both exist
- `ls e2e/clideck-remote-deletion.spec.js e2e/pty-size-locked.spec.js e2e/mobile-touch.spec.js e2e/concurrent-input.spec.js e2e/mobile-viewport.spec.js` → all 5 exist
- `git diff --stat HEAD~0 -- e2e/session-indicator-mutex.spec.js` shows additions (extension)
- `npx vitest run tests/display-sizing.test.js` → green (no regression)
- `npx vitest run tests/sessions-resize.test.js tests/other-client-indicator.test.js` → RED with the expected failure reasons (per CLAUDE.md §1 we DOCUMENT the red, we don't lie that it's green)
- `node --check` succeeds on every new file (syntax sanity)
</verification>

<success_criteria>
Plan 01 is complete when:
- [ ] Both vitest files exist and fail with the documented red-state reasons (RESIZE spy called; updateOtherClientIndicator undefined import)
- [ ] All 5 new Playwright spec files exist with the documented selectors / assertions
- [ ] e2e/session-indicator-mutex.spec.js has the new mutex-non-collision assertion appended
- [ ] tests/display-sizing.test.js remains green (no regression in existing resize-sending assertion per D-05)
- [ ] All new files pass `node --check` (no syntax errors)
- [ ] Commit message records "Wave 0: failing tests authored per TDD §2 — will turn green in Plans 02–05"
</success_criteria>

<output>
Create `.planning/2026-06-02-mobile-desktop-concurrent-access/15-01-SUMMARY.md` when done — record which files were created, the expected red-state reasons for each, and any environmental gaps (e.g. if Chromium libs are missing locally for the Playwright specs to actually execute, note that and defer execution to Plan 06 / 15-VERIFICATION.md).
</output>
