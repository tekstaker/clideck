---
phase: 15-mobile-desktop-concurrent-access
plan: 06
type: execute
wave: 4
depends_on: [12-02, 12-03, 12-04, 12-05]
files_modified:
  - .planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md
autonomous: false
requirements: [R3, R4, R6]
must_haves:
  truths:
    - "Full vitest suite is GREEN (sessions-resize, other-client-indicator, plus all pre-existing tests)"
    - "If Chromium libs are available, all 6 new/extended Playwright specs are GREEN"
    - "If Chromium libs are not available, the gap is documented in 15-VERIFICATION.md per D-19 precedent (Phases 9/10/11)"
    - "Manual real-device verification path for R3 (native soft keyboard on Android/iOS) is documented as the supplementary check per D-14, not a blocker"
    - "Full-repo R1 grep (D-03 pattern) returns zero matches outside CHANGELOG / .planning"
    - "The 8 SPEC.md acceptance criteria are each addressed by at least one verifying artifact (test command or documented manual step)"
  artifacts:
    - path: ".planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md"
      provides: "D-19 verification record — what ran, what didn't, why; supplementary manual check for R3"
      contains: "Acceptance Criteria"
  key_links:
    - from: "15-VERIFICATION.md"
      to: "Each of the 8 SPEC.md acceptance criteria"
      via: "Each AC has a test command, an existing pass, or a documented manual path"
      pattern: "R[1-6]"
---

<objective>
Close the phase with a verification pass. Run every test that this phase introduced, capture the result against each SPEC.md acceptance criterion in `15-VERIFICATION.md` (the D-19 deliverable, modeled on Phases 9/10/11's VERIFICATION.md precedent), and document the supplementary manual-device check for R3.

Per CLAUDE.md §1 the executor MUST actually run the tests — not claim success from code reading. Per §6 the structured completion report (Done / Automated tests run / Manual testing / Testing gaps) drives the SUMMARY content.

Purpose: D-19 deliverable + the Phase 12 sign-off gate.

Output: 15-VERIFICATION.md committed; Phase 12 ready for `/gsd:verify-work` or merge to main / push to Gitea per CLAUDE.md §3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-VALIDATION.md

@.planning/2026-06-02-mobile-desktop-concurrent-access/15-01-SUMMARY.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-02-SUMMARY.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-03-SUMMARY.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-04-SUMMARY.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Run full test suite (vitest + Playwright if Chromium libs available), capture results</name>
  <files>(none modified — diagnostic task only)</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-VALIDATION.md (per-task verification map + Wave 0 list)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md (the 8 Acceptance Criteria block — needed for cross-reference)
    - Each prior plan's SUMMARY (12-01 through 12-05) for the running test status
  </read_first>
  <behavior>
    - Run the full vitest suite and capture pass/fail counts.
    - Attempt to run the full Playwright suite. If Chromium host libs are missing (the well-known sudo-gated case per `clideck-docker/TEST-ENV-DEPS.md`), capture the exact error and STOP — do not pretend it ran.
    - Run the D-03 / RESEARCH.md §1h verification grep across the whole repo and capture the result.
    - Boot `node server.js` for a short window to confirm the server starts cleanly with no error stacks.
  </behavior>
  <action>
    Run each of the following commands in sequence, capturing stdout+stderr to a temp log file for the task SUMMARY:

    1. `npm run test 2>&1 | tee /tmp/12-06-vitest.log` — record the pass count + any failures.
    2. `npx playwright test 2>&1 | tee /tmp/12-06-playwright.log` — record. If Chromium libs are missing the output will include text like `Host system is missing dependencies` or `Looks like Playwright Test or Playwright was just installed`. Capture verbatim. DO NOT claim success from a missing-libs error per CLAUDE.md §1.
    3. Repo-wide R1 verification grep:
       ```
       git grep -nE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|btn-remote|version-remote|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate|remoteVersion|remoteUpdateInfo|remotePreflight|remoteStatusPoll|remoteState|remoteInstalled|remoteModalOpen|remoteLastStatus|btnRemote|remoteModal" -- ':!CHANGELOG.md' ':!.planning/' ':!docker-compose*.yml' ':!Dockerfile*' 2>&1 | tee /tmp/12-06-r1-grep.log
       ```
       The output should be empty.
    4. Server boot smoke: `PORT=4099 CLIDECK_DATA_DIR=$(mktemp -d) timeout 5 node server.js 2>&1 | tee /tmp/12-06-boot.log | head -40`.

    Do NOT modify any source files in this task. Do NOT silently "fix" a failing test if one comes up — surface it to the user via the SUMMARY so they decide whether to extend the plan or accept the gap.
  </action>
  <verify>
    <automated>set -e; ls /tmp/12-06-vitest.log /tmp/12-06-playwright.log /tmp/12-06-r1-grep.log /tmp/12-06-boot.log 2>/dev/null && echo "All 4 capture logs present" || echo "Some capture logs missing — task did not run the required commands"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/12-06-vitest.log` exists and shows a final pass / fail line from vitest (e.g. "Tests  N passed (N)")
    - `/tmp/12-06-playwright.log` exists and shows either a final Playwright pass line OR an explicit Chromium-libs missing / dependency error
    - `/tmp/12-06-r1-grep.log` exists and is either empty (success — zero matches) OR contains the offending file:line entries (which would be a Plan 03/04 escapee)
    - `/tmp/12-06-boot.log` exists and shows the clideck server listening on port 4099 with no error stack
    - tests/sessions-resize.test.js: GREEN (proved by `grep -c "✓\\|PASS" /tmp/12-06-vitest.log` showing this test name as passing)
    - tests/other-client-indicator.test.js: GREEN
    - tests/display-sizing.test.js: GREEN (no regression)
    - No vitest test went from GREEN to RED across the phase
  </acceptance_criteria>
  <done>All 4 capture logs exist; results documented for the Task 6.2 VERIFICATION.md write-up.</done>
</task>

<task type="auto">
  <name>Task 6.2: Author 15-VERIFICATION.md per D-19 (Phases 9/10/11 precedent)</name>
  <files>.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md (8 Acceptance Criteria block — verbatim in the verification doc)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-14, D-18, D-19 — Phase 12 carries a VERIFICATION.md like Phases 9/10/11)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-VALIDATION.md (the verification map this doc captures the actual results for)
    - .planning/phases/2026-05-27-terminal-focus/ — Phase 11 VERIFICATION.md precedent (find via `ls .planning/phases/2026-05-27-terminal-focus/`)
    - .planning/phases/2026-05-27-creator-ergonomics/ — Phase 10 VERIFICATION.md precedent
    - .planning/phases/2026-05-27-terminal-display-sizing/ — Phase 9 VERIFICATION.md precedent
    - /tmp/12-06-vitest.log, /tmp/12-06-playwright.log, /tmp/12-06-r1-grep.log, /tmp/12-06-boot.log (from Task 6.1)
  </read_first>
  <behavior>
    - Create `.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md`.
    - Mirror the document structure of Phase 11's VERIFICATION.md (or Phase 9's if 11's is shorter).
    - Each of the 8 SPEC acceptance criteria gets an explicit row:
        - PASS (with the test command + log location), or
        - PASS via existing test (no new test needed), or
        - DEFERRED to manual / real-device verification (with the exact steps Lance can follow when Android/iOS access is available)
    - Document the R1 grep result (PASS / list of any remaining matches).
    - Document the Chromium-libs status: if Playwright ran, list which specs went green; if it failed on host deps, copy the exact error and note "specs authored but not run locally — see clideck-docker/TEST-ENV-DEPS.md".
    - Per CLAUDE.md §1 do NOT claim a Playwright spec is green if the runner never executed it.
  </behavior>
  <action>
    Read Phase 11's VERIFICATION.md (and 9/10 if available) for the document shape. Typical sections:
    - Frontmatter / heading with phase + date
    - "Test Run Results" table — one row per spec/test with status (✅ / ⚠ / ❌) and a one-line note
    - "Acceptance Criteria Mapping" — each SPEC bullet mapped to its evidence
    - "Manual Verification Steps" — what Lance needs to do himself (e.g. real-device R3 check)
    - "Known Gaps" — anything deferred with rationale

    Author 15-VERIFICATION.md following that shape. Use the actual log content from /tmp/12-06-*.log to fill in.

    For the 8 SPEC.md Acceptance Criteria block (copy the bullet list verbatim into the verification doc and annotate each):

    1. R1 grep returns zero outside CHANGELOG/.planning → cite /tmp/12-06-r1-grep.log result + `e2e/clideck-remote-deletion.spec.js`
    2. Server `resize` handler is no-op (cite `tests/sessions-resize.test.js` + `e2e/pty-size-locked.spec.js`)
    3. PTY cols/rows locked at spawnSession (cite tests + manual / RESEARCH proof)
    4. Two clients attach + concurrent input (cite `e2e/concurrent-input.spec.js` — if Chromium libs unavailable, note as DEFERRED with the manual two-tab path)
    5. Indicator appears/disappears within timing (cite same concurrent-input.spec.js + extended mutex.spec.js)
    6. 375×667 no horizontal overflow + reachable controls (cite `e2e/mobile-viewport.spec.js` walkthrough)
    7. Touch device soft keyboard (cite `e2e/mobile-touch.spec.js` for Playwright emulation gate + DEFER real-device check per D-14)
    8. All existing suites pass + at least one new test for two-client OR resize-no-op (cite vitest run)

    Add the "Manual Verification — R3 real device" section per D-14:
    - Bring up clideck-docker-lance container behind OpenVPN
    - From an Android (Chrome Mobile) or iOS Safari device on the same VPN, navigate to the dashboard URL
    - Tap the terminal pane
    - Confirm native soft keyboard raises
    - Type "echo hello" + Enter
    - Confirm output is visible on phone AND a concurrently-attached desktop
    - Record outcome here when Lance runs the check (this is post-merge, post-deploy)

    Add the "Known Gaps" section:
    - If Chromium host libs not present locally: list the gap + reference clideck-docker/TEST-ENV-DEPS.md
    - If R3 real-device check pending (D-14 supplementary, not a blocker per RESEARCH.md Open Q3)
    - Anything Plan 05 had to use the inline-style fallback for (A1)

    End with a sign-off block:
    ```
    Acceptance: 8 criteria addressed (N PASS automated, M PASS via existing test, P DEFERRED to manual / real-device, Q awaiting Chromium-libs environment)
    Authored: 2026-06-02 (or actual date)
    ```

    Commit per CLAUDE.md §3 / §5 — verbose message, NOT silently pushed to a non-Gitea remote.
  </action>
  <verify>
    <automated>set -e; test -s .planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md; for r in R1 R2 R3 R4 R5 R6; do test "$(grep -c $r .planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md)" -ge 1; done; grep -ciE 'real.device|D-14|manual' .planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md | grep -q -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md` exists and is non-empty
    - The 8 SPEC.md Acceptance Criteria bullets are each addressed (verify by grepping for `R1`..`R6` appearing in the doc + each AC having an explicit status marker like ✅ / ⚠ / ❌)
    - The R1 grep result is captured (PASS or list of remaining matches)
    - The Playwright Chromium-libs status is captured (either green count or the exact dependency error verbatim)
    - The vitest pass count is captured (something like "N tests, N passed" or equivalent)
    - The R3 real-device manual verification path per D-14 is documented as a supplementary check, not a blocker
    - The Plan 05 Tailwind-rebuild-vs-inline-style choice is noted
    - Document follows Phases 9/10/11 shape (compare against `.planning/phases/2026-05-27-terminal-focus/VERIFICATION.md` or similar — same level of structure)
    - Commit message verbose per CLAUDE.md §5, references D-19 + Phase 12 phase identifier
  </acceptance_criteria>
  <done>15-VERIFICATION.md committed; matches Phases 9/10/11 D-19 precedent shape; honest about gaps per CLAUDE.md §1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.3: Lance reviews Phase 12 completion</name>
  <what-built>
    Phase 12 — Mobile + Desktop Concurrent Access — full delivery:
    - R1: clideck-remote retired (server + client; full-repo grep clean)
    - R2: PTY resize is a server-side no-op (D-04 Option A minimum diff)
    - R3: Soft-keyboard happy path verified via Playwright iPhone 12 mobile context (D-13); real-device check is documented as supplementary
    - R4: Two-context concurrent attach + input proved by Playwright spec
    - R5: Server-wide clients.count broadcast wired (handlers.js); client-side indicator on every active + resumable row (terminals.js)
    - R6: Single overflow-x: auto rule on .term-wrap inside existing @media (max-width: 960px); no new ≤480px tier
    - D-19 deliverable: 15-VERIFICATION.md captures actual run results, real-device manual path, and known gaps
  </what-built>
  <how-to-verify>
    1. Skim `.planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md` — confirm each of the 8 SPEC acceptance criteria has a status.
    2. Run `npm run test` locally — confirm full vitest suite passes (expect approximately the prior count + 2 new test files green).
    3. Run the full-repo R1 grep verbatim from 15-CONTEXT.md D-03:
       `git grep -nE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|btn-remote|version-remote|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate|remoteVersion|remoteUpdateInfo|remotePreflight|remoteStatusPoll|remoteState|remoteInstalled|remoteModalOpen|remoteLastStatus|btnRemote|remoteModal" -- ':!CHANGELOG.md' ':!.planning/' ':!docker-compose*.yml' ':!Dockerfile*'`
       — expect zero matches.
    4. Boot the dev container or `npm run dev` and visually confirm: rail bottom has theme + settings only; opening two tabs on different browsers shows the amber two-circle indicator on session rows in both; closing one removes it within ~10s.
    5. (Optional, when clideck-docker-lance is up over VPN) From an Android phone, navigate to the dashboard, tap a terminal pane, confirm soft keyboard raises, type, confirm output mirrors to desktop.
    6. When satisfied, decide: merge to main + push to Gitea per CLAUDE.md §3, OR request adjustments via a continuation phase.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 12 complete, or describe what needs adjustment.</resume-signal>
</task>

</tasks>

<verification>
- `ls .planning/2026-06-02-mobile-desktop-concurrent-access/15-VERIFICATION.md` → exists, non-empty
- `npm run test` → full unit suite green
- Repo R1 grep → zero matches outside CHANGELOG / .planning / docker compose
- Lance's manual verification per Task 6.3 → captured in chat / SUMMARY
</verification>

<success_criteria>
Plan 06 (and Phase 12) is complete when:
- [ ] All capture logs from Task 6.1 exist and were captured from actual test runs (not fabricated per CLAUDE.md §1)
- [ ] 15-VERIFICATION.md exists, matches Phases 9/10/11 D-19 shape, documents each of the 8 SPEC ACs honestly
- [ ] Real-device R3 manual check is documented as a supplementary path (not a blocker)
- [ ] Chromium-libs status is captured exactly as found (no fudging)
- [ ] Lance reviews the deliverable and signals approval or correction (checkpoint Task 6.3)
- [ ] Commit messages across the phase are verbose per CLAUDE.md §5
- [ ] Decision to push (Gitea → autonomous per §3; non-Gitea → wait for Lance) honored
</success_criteria>

<output>
Create `.planning/2026-06-02-mobile-desktop-concurrent-access/15-06-SUMMARY.md` when done — list which acceptance criteria PASS automated vs PASS-via-existing vs DEFERRED vs awaiting-environment, record Lance's checkpoint outcome from Task 6.3, and note whether the phase was pushed to Gitea (autonomous per §3) or left awaiting Lance's review (non-Gitea remote).
</output>
