---
phase: 15-mobile-desktop-concurrent-access
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - sessions.js
autonomous: true
requirements: [R2]
must_haves:
  truths:
    - "Sending `sessions.resize({id, cols, rows})` does not call pty.resize"
    - "Sending a hand-crafted `{type:'resize', id, cols, rows}` WS message does not reshape the PTY"
    - "Existing tests/display-sizing.test.js still passes (client SEND behaviour unchanged per D-05)"
    - "The `resize` message type is still accepted in the WS switch (no throw, no close)"
  artifacts:
    - path: "sessions.js"
      provides: "PTY resize no-op (R2 D-04)"
      contains: "no-op — PTY cols/rows locked at spawnSession"
  key_links:
    - from: "handlers.js:355 case 'resize':"
      to: "sessions.resize() (now no-op)"
      via: "switch case dispatch (unchanged)"
      pattern: "case 'resize':\\s*sessions\\.resize"
    - from: "tests/sessions-resize.test.js"
      to: "sessions.resize"
      via: "spy assertion expect(pty.resize).not.toHaveBeenCalled()"
      pattern: "not\\.toHaveBeenCalled"
---

<objective>
Replace the body of `sessions.resize` at sessions.js:368 with a documented no-op. Per D-04 + 15-RESEARCH.md §3 Option (a): keep the function declaration, keep the export at line 749, keep the `case 'resize':` dispatch at handlers.js:355 unchanged — only the function body changes. This satisfies SPEC R2, the constraint that the message type stays accepted, and minimum diff.

Purpose: Lock PTY cols/rows at session creation. Per-client viewport changes no longer reshape the agent's terminal under another client.

Output: One-file diff to `sessions.js` flips `tests/sessions-resize.test.js` and `e2e/pty-size-locked.spec.js` from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md

@sessions.js
@tests/display-sizing.test.js
@tests/sessions-resize.test.js
@e2e/pty-size-locked.spec.js

<interfaces>
<!-- Key contracts that this plan operates against. Extracted from PATTERNS.md §2 + RESEARCH.md §3. -->

From sessions.js (current):
```javascript
// Line 368 (current — to be replaced)
function resize(msg) { sessions.get(msg.id)?.pty.resize(msg.cols, msg.rows); }
```

From sessions.js (line ~749 — exports — UNCHANGED):
```javascript
module.exports = {
  clients, broadcast, addBroadcastListener, getSessions: () => sessions,
  create, createProgrammatic, resume, restart, input, resize, rename, setTheme, setMute, setProject, setPreview, close, pause, captureToken,
  ...
};
```

From handlers.js (line ~355 — dispatch — UNCHANGED):
```javascript
case 'resize':               sessions.resize(msg); break;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Replace sessions.resize body with documented no-op</name>
  <files>sessions.js</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md (R2 + the "resize message type must remain accepted" constraint)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-04, D-05, D-06)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§3 minimum-diff path, §10 G2 risk)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§2a current code + §2c splice)
    - sessions.js (read full file — confirm line 368 exact content and verify the exports list still includes `resize`)
    - tests/sessions-resize.test.js (created in Plan 01 — Wave 0 — verify the assertions match what this task makes green)
    - tests/display-sizing.test.js (verify the existing assertions about client-side SENDING `resize` are unchanged)
  </read_first>
  <behavior>
    - After this task, calling `sessions.resize({id: 'x', cols: 40, rows: 10})` against a session whose `pty.resize` is a spy MUST NOT invoke that spy
    - `sessions.resize` MUST still exist as a function and remain in the exports at sessions.js:~749
    - `sessions.resize({})` (no id, no cols, no rows) MUST NOT throw
    - `sessions.resize({id: 'nonexistent'})` MUST NOT throw
    - The `case 'resize':` dispatch in handlers.js:~355 remains unchanged (still accepts the message type)
    - tests/display-sizing.test.js continues to pass (client behaviour unchanged per D-05)
  </behavior>
  <action>
    Locate `sessions.js:368` (current content per PATTERNS.md §2a: `function resize(msg) { sessions.get(msg.id)?.pty.resize(msg.cols, msg.rows); }`). Confirm the line by reading sessions.js first — if line drift has occurred since 15-RESEARCH.md was written (commit 9f6a111), grep for `function resize(msg)` to find the actual line.

    Replace the entire one-line function body with the multi-line no-op form from PATTERNS.md §2c. The exact replacement is the form documented in 15-RESEARCH.md "Code Examples → Server: D-04 resize no-op" and PATTERNS.md §2c — function name `resize`, single underscore-prefixed parameter `_msg`, function body empty except for a multi-line comment explaining (a) PTY size is locked at session creation per Phase 12 R2, (b) the message type stays accepted for older clients per the SPEC constraint, (c) per-client viewport changes do not reshape the PTY, (d) reference to `.planning/2026-06-02-mobile-desktop-concurrent-access/` for context.

    Do NOT modify any other line in sessions.js:
    - The exports list at line ~749 stays as-is (include `resize` in the destructured export).
    - The `spawnSession(id, cmd, parts, cwd, name, themeId, commandId, savedToken, projectId, cols, rows)` signature at line 85 stays unchanged (per D-06).
    - The two `spawnSession` call sites at lines ~220 and ~496 stay unchanged.
    - All other functions (`input`, `create`, `resume`, `restart`, `close`, `pause`, etc.) stay unchanged.

    Do NOT modify handlers.js:355 — the `case 'resize': sessions.resize(msg); break;` dispatch stays exactly as it is. Plan 03 modifies a DIFFERENT region of handlers.js (~lines 46-98, 246-248, 251-252, 601-650, 658) but NOT line 355.

    Do NOT touch any client code (terminals.js continues to send `{type:'resize'}` messages on viewport changes per D-05).
  </action>
  <verify>
    <automated>npx vitest run tests/sessions-resize.test.js tests/display-sizing.test.js 2>&1 | tee /tmp/12-02-task1-vitest.log; echo "Expected: both files exit 0 (sessions-resize was RED in Plan 01, now GREEN; display-sizing was GREEN, still GREEN)."</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function resize" sessions.js` returns exactly one line (the no-op function declaration)
    - `grep -A 10 "function resize" sessions.js | grep -c "pty.resize"` returns 0 (the call to pty.resize is gone)
    - `grep "function resize" sessions.js | grep -c "_msg"` returns 1 (the underscore-prefixed parameter convention is applied)
    - `grep -A 8 "function resize" sessions.js | grep -c "Phase 12\\|R2\\|locked at"` ≥ 1 (the no-op rationale comment is present)
    - `grep -E "resize," sessions.js | grep -c "module.exports\\|resize" ` ≥ 1 (export at line ~749 still includes `resize`)
    - `grep -n "case 'resize'" handlers.js` still returns line ~355 (dispatch unchanged)
    - `npx vitest run tests/sessions-resize.test.js` exits 0 (formerly RED, now GREEN)
    - `npx vitest run tests/display-sizing.test.js` exits 0 (no regression — client still sends resize, just nobody listens server-side)
    - `node --check sessions.js` succeeds (no syntax errors)
    - Verbose commit message references Phase 12 R2 + D-04 per CLAUDE.md §5
  </acceptance_criteria>
  <done>sessions.js:368 body replaced; tests/sessions-resize.test.js GREEN; tests/display-sizing.test.js still GREEN; handlers.js unchanged.</done>
</task>

</tasks>

<verification>
Final checks:
- `npx vitest run tests/sessions-resize.test.js tests/display-sizing.test.js` → both GREEN (Plan 01's RED on sessions-resize.test.js now flips)
- `git diff --stat sessions.js` shows a small diff (≤ 10 lines changed, all in the resize function body)
- `git diff -- handlers.js` shows NO changes (handlers.js is Plan 03's territory)
- Run the full unit suite to make sure nothing else regresses: `npm run test`
</verification>

<success_criteria>
Plan 02 is complete when:
- [ ] sessions.js:368 contains the no-op `function resize(_msg) { /* ... comment ... */ }` per PATTERNS.md §2c
- [ ] The exports list at sessions.js:~749 still includes `resize`
- [ ] handlers.js is unchanged in this plan
- [ ] tests/sessions-resize.test.js flips from RED → GREEN
- [ ] tests/display-sizing.test.js stays GREEN
- [ ] Full vitest suite passes (`npm run test`)
- [ ] Commit message is verbose per CLAUDE.md §5, references R2 / D-04, and notes "Plan 01's sessions-resize.test.js now flips green"
</success_criteria>

<output>
Create `.planning/2026-06-02-mobile-desktop-concurrent-access/15-02-SUMMARY.md` when done — record the exact line replaced, the test result (RED → GREEN flip on sessions-resize), and confirm no regression on display-sizing.
</output>
