---
phase: 15-mobile-desktop-concurrent-access
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-03, 12-04]
files_modified:
  - public/js/state.js
  - public/js/terminals.js
  - public/js/app.js
  - public/index.html
  - public/tailwind.css
autonomous: true
requirements: [R5, R6]
must_haves:
  truths:
    - "`state.otherClientsConnected` exists as a boolean field in state.js"
    - "`updateOtherClientIndicator(count)` is exported from public/js/terminals.js"
    - "Calling updateOtherClientIndicator(2) removes `.hidden` from every `.other-client-indicator`; updateOtherClientIndicator(1) re-adds it"
    - "Session rows (active + resumable) include the indicator span between `.name` and `.session-time`"
    - "New rows added AFTER a second client connects render the indicator without `.hidden` (G9 mitigation)"
    - "app.js's WS onmessage switch dispatches `case 'clients.count':` to updateOtherClientIndicator"
    - "When a second client connects, the indicator becomes visible on every session row in the existing client"
    - "When the second client disconnects, the indicator returns to `.hidden` on every row"
    - "The indicator uses `text-amber-400` (Tailwind), distinct from the existing `.unread-dot` (`bg-blue-500`)"
    - "Indicator markup has `title='Another client is connected'` and matching `aria-label`"
    - "Inside the existing `@media (max-width: 960px)` block, `.term-wrap { overflow-x: auto; -webkit-overflow-scrolling: touch; }` exists"
    - "public/tailwind.css contains a usable `text-amber-400` rule (rebuilt) OR the markup uses inline `style='color:#FBBF24'` as fallback per A1/G11"
  artifacts:
    - path: "public/js/state.js"
      provides: "otherClientsConnected flag (R5 D-10)"
      contains: "otherClientsConnected"
    - path: "public/js/terminals.js"
      provides: "Indicator markup in both row templates + updateOtherClientIndicator export"
      contains: "other-client-indicator"
    - path: "public/js/app.js"
      provides: "WS case 'clients.count' arm + import of updateOtherClientIndicator"
      contains: "clients.count"
    - path: "public/index.html"
      provides: "Responsive CSS — .term-wrap overflow-x:auto in @media (max-width:960px)"
      contains: ".term-wrap"
    - path: "public/tailwind.css"
      provides: "Compiled text-amber-400 utility (rebuilt) OR fallback handled inline"
      contains: "amber"
  key_links:
    - from: "handlers.js sessions.broadcast({type:'clients.count'})"
      to: "public/js/app.js case 'clients.count':"
      via: "WebSocket message dispatch"
      pattern: "case 'clients\\.count'"
    - from: "public/js/app.js case 'clients.count':"
      to: "public/js/terminals.js updateOtherClientIndicator export"
      via: "imported helper call"
      pattern: "updateOtherClientIndicator\\("
    - from: "public/js/terminals.js addTerminal row template"
      to: "DOM .other-client-indicator span"
      via: "innerHTML construction"
      pattern: "other-client-indicator"
    - from: "public/js/terminals.js buildResumableRow template"
      to: "DOM .other-client-indicator span"
      via: "innerHTML construction"
      pattern: "other-client-indicator"
---

<objective>
Wire the R5 "other client connected" indicator end-to-end on the client side: (1) add the `state.otherClientsConnected` flag, (2) splice the locked indicator markup into BOTH session-row templates in terminals.js, (3) export `updateOtherClientIndicator(count)`, (4) add the `case 'clients.count':` arm in app.js, (5) rebuild Tailwind so `text-amber-400` is in the compiled CSS (G11/A1 mitigation), and (6) add the single R6 responsive CSS rule `.term-wrap { overflow-x: auto; ... }` inside the existing `@media (max-width: 960px)` block.

Purpose: Realize R5 end-to-end on the client (Plan 03 already added the server broadcast) AND R6's single CSS rule. Per the dependency graph this plan needs Plan 03's server broadcast to receive AND Plan 04's state.js cleanup (to avoid merge conflict on state.js).

Output: 5-file diff that flips `tests/other-client-indicator.test.js`, `e2e/concurrent-input.spec.js`, `e2e/mobile-viewport.spec.js`, and the extended assertion in `e2e/session-indicator-mutex.spec.js` from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md

@public/js/state.js
@public/js/terminals.js
@public/js/app.js
@public/index.html
@tailwind.config.js
@package.json

@tests/other-client-indicator.test.js
@e2e/concurrent-input.spec.js
@e2e/mobile-viewport.spec.js

<interfaces>
<!-- Key interfaces this plan creates. Other plans / tests consume them. -->

From public/js/state.js (post-Plan-04 — Plan 04 removed remoteVersion):
Existing fields are: ws, terms (Map), active, cfg, themes, presets, resumable, filter, pills (Map), activePill, transcriptCache.
THIS PLAN ADDS: `otherClientsConnected: false,` field.

From public/js/terminals.js (post-this-plan):
- New export `updateOtherClientIndicator(count)` that sets state.otherClientsConnected = count > 1 and toggles `.hidden` on every `.other-client-indicator` element.

Indicator markup (LOCKED by 12-UI-SPEC § "DOM contract" — copy verbatim, do not vary):
- Wrapper span class list: `other-client-indicator` + conditional ` hidden` (from G9 ternary) + `flex-shrink-0` + `text-amber-400`
- Wrapper attributes: `title="Another client is connected"`, `aria-label="Another client is connected"`
- Inner SVG: width=10 height=10 viewBox="0 0 16 16" fill=none stroke=currentColor stroke-width=1.5 aria-hidden=true
- Two `<circle>` elements: cx=6 cy=8 r=3.5 AND cx=10 cy=8 r=3.5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: Add otherClientsConnected flag and updateOtherClientIndicator export + splice indicator markup into both row templates</name>
  <files>public/js/state.js, public/js/terminals.js</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md ("Other-client indicator — exact contract" — DOM contract, Resumable-rows, Accessibility, Copywriting Contract)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-08, D-09, D-10, D-11)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§4 insertion points, §5 state-flag plumbing, §10 G9 risk, Code Examples → Client section)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§6 state.js, §7a current row template, §7b locked splice, §7c helper export, §7d resumable splice)
    - public/js/state.js (full file — verify Plan 04 already removed remoteVersion)
    - public/js/terminals.js (targeted reads: lines 1-30 imports, 500-540 addTerminal template, 700-740 click handler context, 1300-1350 buildResumableRow)
    - tests/other-client-indicator.test.js (Wave 0 — verifies what this task makes green)
  </read_first>
  <behavior>
    public/js/state.js:
    - Add `otherClientsConnected: false,` field just before the closing `};` of the state literal.

    public/js/terminals.js:
    - Insert the LOCKED indicator markup between `.name` and `.session-time` inside `addTerminal`'s row template (around line 514 per RESEARCH.md §4 / PATTERNS.md §7b).
    - Insert the SAME markup between `.resumable-name` and the timestamp span inside `buildResumableRow` (around line 1322 per RESEARCH.md §4 / PATTERNS.md §7d).
    - Add a new export `updateOtherClientIndicator(count)` adjacent to existing exports (search with grep for `export function` to find region).
    - G9 mitigation (per RESEARCH.md §10 G9): both row templates MUST build the indicator with `state.otherClientsConnected ? '' : ' hidden'` so a row added AFTER a second client connected renders the indicator already visible.
    - Do NOT enable the D-15 touchstart contingency line — leave commented per PATTERNS.md §7e.
  </behavior>
  <action>
    Sub-task 5.1a — public/js/state.js:

    Read the full file (~14-15 lines post-Plan-04). Confirm `remoteVersion` is absent. Add `otherClientsConnected: false,` as a new field just before the closing `};` of the `state` literal. Resulting shape matches PATTERNS.md §6b.

    Sub-task 5.1b — public/js/terminals.js indicator splice in addTerminal:

    Read lines 500-540 to locate the row template (PATTERNS.md §7a). The splice site is inside `<div class="flex items-baseline gap-2">...</div>` between the `<span class="name ...">` and `<span class="session-time ...">` (line 514-516 area).

    Insert the locked indicator span. The exact form per UI-SPEC § "DOM contract" + PATTERNS.md §7b: a `<span>` with class list `other-client-indicator${state.otherClientsConnected ? '' : ' hidden'} flex-shrink-0 text-amber-400`, attributes `title="Another client is connected"` and `aria-label="Another client is connected"`. Inside the span, an SVG element width="10" height="10" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true" containing two `<circle>` elements (cx="6" cy="8" r="3.5" and cx="10" cy="8" r="3.5").

    DO NOT paraphrase title or aria-label. DO NOT alter SVG coordinates. DO NOT change color from text-amber-400 (Task 5.3 handles CSS rebuild for that class).

    Sub-task 5.1c — public/js/terminals.js indicator splice in buildResumableRow:

    Read lines 1300-1340 (PATTERNS.md §7d). The structure mirrors addTerminal's top row. Insert the SAME locked indicator markup between `<span class="resumable-name ...">` and the timestamp `<span class="text-[11px] ...">${time}</span>`. Identical ternary for G9.

    Sub-task 5.1d — public/js/terminals.js new export updateOtherClientIndicator:

    Per PATTERNS.md §7c + RESEARCH.md "Code Examples → Client: updateOtherClientIndicator helper", add an exported function adjacent to the existing `focusTerminal` export. Function body: set `state.otherClientsConnected = count > 1`, then iterate `document.querySelectorAll('.other-client-indicator')` calling `el.classList.toggle('hidden', !state.otherClientsConnected)` on each.

    Verify the `state` import is already present at top of terminals.js (it is, since templates use state).

    Verify with: `node --check public/js/terminals.js`. Then `npx vitest run tests/other-client-indicator.test.js` — Wave 0 test should now flip RED to GREEN.
  </action>
  <verify>
    <automated>set -e; node --check public/js/state.js; node --check public/js/terminals.js; test "$(grep -c otherClientsConnected public/js/state.js)" = "1"; test "$(grep -c updateOtherClientIndicator public/js/terminals.js)" -ge 2; test "$(grep -c other-client-indicator public/js/terminals.js)" -ge 2; test "$(grep -c 'Another client is connected' public/js/terminals.js)" -ge 2; npx vitest run tests/other-client-indicator.test.js 2>&1 | tee /tmp/12-05-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/js/state.js` exits 0
    - `node --check public/js/terminals.js` exits 0
    - `grep -c "otherClientsConnected" public/js/state.js` returns 1
    - `grep -c "remoteVersion" public/js/state.js` returns 0 (Plan 04 removed it)
    - `grep -c "updateOtherClientIndicator" public/js/terminals.js` returns ≥ 2 (one declaration plus references)
    - `grep -c "other-client-indicator" public/js/terminals.js` returns ≥ 2 (active row + resumable row)
    - `grep -c "Another client is connected" public/js/terminals.js` returns ≥ 2 (one title plus one aria-label per row template, two templates means 4 occurrences, minimum 2)
    - `grep -c "text-amber-400" public/js/terminals.js` returns ≥ 2 (both row templates)
    - `grep -c "cx=\"6\" cy=\"8\"" public/js/terminals.js` returns ≥ 2 (locked SVG circle)
    - `grep -c "cx=\"10\" cy=\"8\"" public/js/terminals.js` returns ≥ 2 (locked SVG circle)
    - `grep -c "state.otherClientsConnected ? '' : ' hidden'" public/js/terminals.js` returns ≥ 2 (G9 ternary in both templates)
    - `npx vitest run tests/other-client-indicator.test.js` exits 0 (formerly RED, now GREEN)
    - `npx vitest run tests/sessions-resize.test.js tests/display-sizing.test.js` both exit 0 (no regression)
  </acceptance_criteria>
  <done>state.js has otherClientsConnected; terminals.js has the indicator markup in both row templates and exports updateOtherClientIndicator; Wave 0 vitest tests for R5 are green.</done>
</task>

<task type="auto">
  <name>Task 5.2: Add case 'clients.count' arm to app.js's WS onmessage switch + import updateOtherClientIndicator</name>
  <files>public/js/app.js</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md ("Other-client indicator — exact contract")
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-09, D-10, D-11)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§5 state flag + WS handler precedent, "Code Examples → Client: app.js WS handler arm")
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§4 — Shared 2 state.X = value + DOM toggle, §4c splice for D-10)
    - public/js/app.js (targeted reads: lines 1-40 for imports, 110-220 for onmessage switch — verify Plan 04 already removed the 7 remote.* arms and the send remote.status line)
    - public/js/terminals.js (verify updateOtherClientIndicator export landed in task 5.1)
  </read_first>
  <behavior>
    - The existing `import` from `./terminals.js` at the top of app.js gains `updateOtherClientIndicator` in its destructured import list.
    - The `state.ws.onmessage` switch gains a new arm `case 'clients.count': updateOtherClientIndicator(msg.count); break;` placed near `case 'closed':` and `case 'sessions':` per PATTERNS.md §4c (the alphabetical-ish neighborhood).
    - No other modifications to app.js — the 7 `case 'remote.*':` arms are already gone (Plan 04) and the `send remote.status` line is already gone (Plan 04).
    - Booting `node server.js` and loading the dashboard in a browser shows no console errors (the new arm dispatches correctly when the server broadcast from Plan 03 arrives).
  </behavior>
  <action>
    Read the top of public/js/app.js (lines 1-40) to locate the existing import from `./terminals.js`. The current import contains identifiers such as `addTerminal, removeTerminal, setHasToken, focusTerminal, ...` (the exact list will be confirmed at read time). Add `updateOtherClientIndicator` to the destructured import list. Per PATTERNS.md §4c the import edit is one line.

    Read lines 110-220 (where the `state.ws.onmessage` switch lives). Locate the neighborhood of `case 'closed':` (line ~199-206 per RESEARCH.md §5) and `case 'session.token':` (line ~207-213 per PATTERNS.md §4b). Insert the new arm between them (or in a similar alphabetical-ish slot):

        case 'clients.count':
          updateOtherClientIndicator(msg.count);
          break;

    Verify with `node --check public/js/app.js`. Verify the arm matches the pattern by grepping `grep -n "case 'clients.count'" public/js/app.js` (expect one match).

    Boot the server briefly: `PORT=4099 CLIDECK_DATA_DIR=$(mktemp -d) timeout 5 node server.js | head -20` (clean startup expected, since Plan 03's broadcast won't fire without a WS client).

    Run the full vitest suite: `npm run test`. tests/other-client-indicator.test.js stays GREEN (Task 5.1), no regressions expected.
  </action>
  <verify>
    <automated>set -e; node --check public/js/app.js; test "$(grep -c \"case 'clients.count'\" public/js/app.js)" = "1"; test "$(grep -c \"updateOtherClientIndicator\" public/js/app.js)" -ge 2; npm run test 2>&1 | tee /tmp/12-05-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/js/app.js` exits 0
    - `grep -c "case 'clients.count'" public/js/app.js` returns 1
    - `grep -c "updateOtherClientIndicator" public/js/app.js` returns ≥ 2 (one import plus one call)
    - The import line for `./terminals.js` (located via `grep -n "from './terminals.js'\\|from \\\"./terminals.js\\\"" public/js/app.js`) contains `updateOtherClientIndicator` in its destructured list
    - No other changes to app.js (R1 deletions already done in Plan 04 — verify no diff drift)
    - `npm run test` exits 0 (no unit regression)
    - Server boots cleanly: `timeout 5 node server.js` produces no error stack
  </acceptance_criteria>
  <done>app.js dispatches case 'clients.count' to updateOtherClientIndicator; unit suite green.</done>
</task>

<task type="auto">
  <name>Task 5.3: Add R6 responsive CSS rule to index.html + rebuild tailwind.css for text-amber-400</name>
  <files>public/index.html, public/tailwind.css</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md ("Phone-viewport responsive contract" — Terminal pane overflow LOCKED decision, Cross-mode contingency)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-16, D-07)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§10 G10 + G11 + A1, Code Examples → CSS section, Common Pitfalls → Pitfall 3)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§3a existing block, §3b splice, §8 tailwind rebuild)
    - public/index.html (targeted read: lines 60-130 — the existing @media (max-width: 960px) block; verify Plan 04 deletions stayed put)
    - tailwind.config.js (verify content glob scans public/**/*.{html,js})
    - package.json (verify the build:css script: `tailwindcss -i src/input.css -o public/tailwind.css --minify`)
  </read_first>
  <behavior>
    public/index.html:
    - Inside the existing `@media (max-width: 960px) { ... }` block at lines 60-127, append a `.term-wrap { overflow-x: auto; -webkit-overflow-scrolling: touch; }` rule.
    - Do NOT introduce a new `@media (max-width: 480px)` tier — UI-SPEC and D-16 lock this to extending the existing 960px breakpoint.

    public/tailwind.css:
    - After Task 5.1's indicator markup added `text-amber-400` to terminals.js, run `npm run build:css` so the Tailwind content-scan picks it up and includes the rule in the compiled output.
    - If `npm run build:css` is unavailable or fails for any environmental reason (no Tailwind CLI on PATH, etc.), apply the A1/G11 contingency from RESEARCH.md: replace `text-amber-400` in BOTH terminals.js splice sites with inline `style="color:#FBBF24"` and skip the rebuild. Document the choice in the task SUMMARY.
  </behavior>
  <action>
    Sub-task 5.3a — public/index.html R6 responsive CSS rule:

    Read lines 60-130 to confirm the existing `@media (max-width: 960px) { ... }` block (PATTERNS.md §3a). The block currently ends around line 126 with `#folder-picker > div { width: 100%; max-width: 420px; }` and closes at line 127.

    Append (inside the closing `}` of the media query, after the existing `#folder-picker > div` rule):

        .term-wrap {
          overflow-x: auto;
          -webkit-overflow-scrolling: touch;
        }

    This is the only CSS rule added in this plan. The selector `.term-wrap` is the wrapper that hosts every xterm grid (per PATTERNS.md §3b — confirmed at terminals.js line ~537). Per UI-SPEC § "Terminal pane overflow — LOCKED decision" this delivers the horizontal-scroll behavior phone clients need when the locked PTY width exceeds the viewport, without reshaping the PTY.

    Verify with `grep -n "term-wrap" public/index.html` (expect ≥ 1 match in the CSS region, plus possibly other matches outside the media block) and `grep -c "overflow-x: auto" public/index.html` (expect ≥ 1).

    Sub-task 5.3b — public/tailwind.css rebuild for text-amber-400:

    First check whether text-amber-400 is already in the compiled CSS:
        grep -c "amber-400\\|amber" public/tailwind.css
    Per RESEARCH.md A1 / G11 / Pitfall 3 this likely returns 0 today.

    Run the tailwind build via the package.json script:
        npm run build:css
    This invokes `tailwindcss -i src/input.css -o public/tailwind.css --minify` per the package.json. After the build:
        grep -c "amber\\|#fbbf24\\|#FBBF24" public/tailwind.css
    should return ≥ 1 (case-insensitive; the minified output may emit the color value directly or as a class rule).

    If `npm run build:css` fails (Tailwind CLI not available, source file missing, etc.):
    - Document the failure mode in the task SUMMARY.
    - Apply the A1/G11 inline-style contingency: edit public/js/terminals.js to replace each `text-amber-400` occurrence in the indicator span with an inline `style="color:#FBBF24"` attribute (per UI-SPEC § "Cross-mode (dark / light) verification contract" → "Contingency for light mode" + RESEARCH.md A1 + PATTERNS.md §8 fallback).
    - Re-run `npx vitest run tests/other-client-indicator.test.js` to confirm the toggle still works (the unit test asserts on `.hidden` class, not on color, so it stays green).

    Commit BOTH the index.html change AND the tailwind.css rebuild (or the inline-style fallback) in the same commit so the visual indicator works correctly when next loaded in the browser.
  </action>
  <verify>
    <automated>set -e; test "$(grep -c 'term-wrap' public/index.html)" -ge 1; test "$(grep -c 'overflow-x: auto' public/index.html)" -ge 1; AMBER_IN_CSS=$(grep -ciE 'amber|#fbbf24' public/tailwind.css 2>/dev/null || echo 0); AMBER_INLINE=$(grep -c '#FBBF24' public/js/terminals.js 2>/dev/null || echo 0); echo "amber in tailwind.css: $AMBER_IN_CSS; #FBBF24 inline in terminals.js: $AMBER_INLINE"; test "$AMBER_IN_CSS" -ge 1 -o "$AMBER_INLINE" -ge 2; npm run test 2>&1 | tee /tmp/12-05-task3.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "term-wrap" public/index.html` returns ≥ 1 in the @media (max-width: 960px) region (verify line proximity by grepping for both `@media (max-width: 960px)` and `term-wrap` and confirming they are within ~70 lines)
    - `grep -c "overflow-x: auto" public/index.html` returns ≥ 1
    - `grep -c "-webkit-overflow-scrolling: touch" public/index.html` returns ≥ 1
    - NO new `@media (max-width: 480px)` block exists in public/index.html (confirms D-16 was honored)
    - EITHER `grep -ciE "amber|#fbbf24" public/tailwind.css` returns ≥ 1 (the rebuild succeeded) OR `grep -c "#FBBF24" public/js/terminals.js` returns ≥ 2 (inline-style fallback applied to both row templates)
    - `npm run test` exits 0 (unit suite still green)
    - `node --check public/js/terminals.js` exits 0 if the fallback was applied
    - Summary task documents which path was taken (Tailwind rebuild vs inline-style fallback)
  </acceptance_criteria>
  <done>index.html has the .term-wrap overflow rule inside the existing 960px block; tailwind.css contains text-amber-400 OR terminals.js uses the inline-style fallback; unit suite green.</done>
</task>

</tasks>

<verification>
- All 5 files modified (state.js, terminals.js, app.js, index.html, tailwind.css OR terminals.js if fallback was applied)
- `npx vitest run` → full unit suite GREEN including tests/other-client-indicator.test.js (was RED in Plan 01)
- `npm run test` → exit 0
- Boot server briefly, load dashboard in a browser if possible (or trust the Playwright specs in Plan 06)
- If Chromium libs available: `npx playwright test e2e/concurrent-input.spec.js e2e/mobile-viewport.spec.js e2e/session-indicator-mutex.spec.js` → green. If not, document the deferral in 15-05-SUMMARY.md
- Grep audit: `grep -c "other-client-indicator" public/js/terminals.js` ≥ 2; `grep -c "case 'clients.count'" public/js/app.js` = 1; `grep -c "otherClientsConnected" public/js/state.js` = 1
</verification>

<success_criteria>
Plan 05 is complete when:
- [ ] state.js has the otherClientsConnected: false field
- [ ] terminals.js has the locked indicator markup in addTerminal AND buildResumableRow (both with the G9 ternary)
- [ ] terminals.js exports updateOtherClientIndicator(count)
- [ ] app.js dispatches case 'clients.count': updateOtherClientIndicator(msg.count)
- [ ] index.html has the .term-wrap overflow-x rule inside the existing @media (max-width: 960px) block (no new 480px tier)
- [ ] tailwind.css contains text-amber-400 (rebuilt) OR terminals.js uses inline style="color:#FBBF24" fallback
- [ ] tests/other-client-indicator.test.js flips RED → GREEN
- [ ] Full unit suite passes (npm run test)
- [ ] If Chromium libs available: concurrent-input.spec.js + mobile-viewport.spec.js + extended mutex.spec.js all green
- [ ] Commit message verbose per CLAUDE.md §5; references R5 + R6 + D-08..D-11 + D-16
</success_criteria>

<output>
Create `.planning/2026-06-02-mobile-desktop-concurrent-access/15-05-SUMMARY.md` when done — record which files were modified, the exact splice points, the chosen path for Tailwind rebuild vs inline-style fallback, the test results (which RED tests flipped green, which stayed green), and any Chromium-libs deferral if relevant.
</output>
