---
phase: 15-mobile-desktop-concurrent-access
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - public/index.html
  - public/js/app.js
  - public/js/settings.js
  - public/js/state.js
autonomous: true
requirements: [R1]
must_haves:
  truths:
    - "`#btn-remote` rail launcher is absent from the DOM"
    - "`#remote-modal` block is absent from the DOM"
    - "`#version-remote` span is absent from the DOM"
    - "No `send({type:'remote.status'})` call in app.js"
    - "No `case 'remote.*':` arms in app.js's WS switch"
    - "The 340-line remote driver block (lines 1523-1863) is gone from app.js"
    - "`state.remoteVersion` field removed from state.js (the otherClientsConnected field is added in Plan 05)"
    - "`version-remote` reads removed from settings.js"
    - "Dashboard loads with no console errors"
    - "Full repo verification grep (R1 D-03) returns zero matches outside CHANGELOG/.planning"
  artifacts:
    - path: "public/index.html"
      provides: "R1 client-side DOM deletion sweep"
    - path: "public/js/app.js"
      provides: "R1 client-side WS handler + driver block deletion"
    - path: "public/js/settings.js"
      provides: "R1 client-side version-footer cleanup"
    - path: "public/js/state.js"
      provides: "R1 state schema cleanup (remoteVersion removed)"
  key_links:
    - from: "public/index.html (post-deletion)"
      to: "e2e/clideck-remote-deletion.spec.js"
      via: "page.locator('#btn-remote').toHaveCount(0) etc."
      pattern: "toHaveCount\\(0\\)"
    - from: "public/js/app.js (post-deletion)"
      to: "smoke.spec.js"
      via: "no console.error on load (no dangling getElementById)"
      pattern: "errors.*toEqual\\(\\[\\]\\)"
---

<objective>
Complete the R1 client-side deletion sweep — remove every reference to `clideck-remote`, `#remote-modal`, `#btn-remote`, `#version-remote`, the WS `remote.*` message types, the 340-line driver block in app.js, the version-remote read in settings.js, and the `state.remoteVersion` field. Per D-01 the fork should no longer "know" about clideck-remote after this phase.

Purpose: Realize the user-facing UI deletion contract from 15-UI-SPEC.md ("Deletion contract" section) and the inventory from 15-RESEARCH.md §1b-1e.

Output: 4-file diff. `e2e/clideck-remote-deletion.spec.js` (Wave 0) flips RED → GREEN; `e2e/smoke.spec.js` continues to assert `errors == []`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/2026-06-02-mobile-desktop-concurrent-access/SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md
@.planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md

@public/index.html
@public/js/app.js
@public/js/settings.js
@public/js/state.js
@e2e/clideck-remote-deletion.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Delete remote DOM markup from public/index.html</name>
  <files>public/index.html</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md ("Deletion contract" section — verbatim location table)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-01, D-02, D-03)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§1b — line numbers verified at 9f6a111)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§3 + §3c deletion table)
    - public/index.html (full file — confirm line numbers, especially the modal block end at 493 not ~480 per RESEARCH.md §1b)
  </read_first>
  <behavior>
    After this task:
    - `grep -c "btn-remote" public/index.html` returns 0
    - `grep -c "remote-modal" public/index.html` returns 0
    - `grep -c "version-remote" public/index.html` returns 0
    - The rail's bottom-section icon stack shrinks from 3 to 2 (theme + settings) — flex handles reflow automatically per 12-UI-SPEC §"Deletion contract" → "Rail layout reflow"
    - The dashboard still loads cleanly (no broken element references)
  </behavior>
  <action>
    Read public/index.html in full (it is ~700-1000 lines based on 15-RESEARCH.md context). Confirm the three deletion targets via grep BEFORE editing:
    - `grep -n "btn-remote" public/index.html` — expect a single match around line 154
    - `grep -n "remote-modal" public/index.html` — expect one match around line 406 (the opening `<div id="remote-modal">`)
    - `grep -n "version-remote" public/index.html` — expect one match around line 249

    Edit 1 — Delete the `<button id="btn-remote">…</button>` element entirely (lines 154-156 per RESEARCH.md §1b / PATTERNS.md §3c). The `<div class="flex-1"></div>` spacer at line 153 STAYS (it pushes theme/settings to the rail bottom; deletion leaves the existing 2-icon stack flex-positioned correctly). Per UI-SPEC "Deletion contract → Rail layout reflow" no layout fix is needed.

    Edit 2 — Delete the line containing `<div class="text-slate-600">clideck remote version: <span id="version-remote" class="text-slate-500"></span></div>` (line 249 per RESEARCH.md §1b). KEEP the surrounding lines including `version-clideck` (line 248) — that's the surviving version-footer entry. Per PATTERNS.md §3c only line 249 is deleted; the `#version-footer` container stays.

    Edit 3 — Delete the entire `<!-- Remote modal -->` comment + `<div id="remote-modal">…</div>` block. The block STARTS at line 405 (the comment) or line 406 (the opening `<div>`), and ENDS at line 493 (the closing `</div>` of the modal root, immediately before `<!-- Confirmation dialog -->` at line 495). Verify the exact end by grep + Read: `grep -n "remote-modal\\|Confirmation dialog\\|<!-- Remote" public/index.html` to locate boundaries precisely before deleting. Delete the comment line + the entire `<div id="remote-modal">…</div>` block.

    Do NOT modify the `@media (max-width: 960px)` block at line 60 in this task — that's Plan 05's territory (R6 CSS rule).

    Verify with the final grep: `grep -nE "btn-remote|remote-modal|version-remote" public/index.html` returns zero. Run `node -e "const fs=require('fs'); const html=fs.readFileSync('public/index.html','utf8'); console.log(html.length, 'bytes,', html.split('\\n').length, 'lines')"` to confirm the file shrunk by roughly 90 lines (88-line modal + 1 button + 1 version-remote line).
  </action>
  <verify>
    <automated>set -e; for sel in btn-remote remote-modal version-remote; do COUNT=$(grep -c "$sel" public/index.html || echo 0); echo "$sel matches: $COUNT (expect 0)"; test "$COUNT" = "0"; done; echo "public/index.html R1 deletions verified"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "btn-remote" public/index.html` returns 0
    - `grep -c "remote-modal" public/index.html` returns 0
    - `grep -c "version-remote" public/index.html` returns 0
    - `grep -c "Mobile Remote" public/index.html` returns 0 (matches the UI-SPEC "Removed copy" list)
    - `grep -c "version-clideck" public/index.html` returns ≥ 1 (PRESERVED — desktop version row still there)
    - File size reduced by approximately 88-92 lines (the modal block ~88 lines + button ~3 lines + version-remote 1 line)
    - HTML parses without error: `node -e "const fs=require('fs'); require('htmlparser2').parseDocument(fs.readFileSync('public/index.html','utf8'))"` succeeds (or use a simpler `node --check`-like check via reading whole file). If htmlparser2 isn't available, this is informational only — the Playwright smoke spec is the real gate.
  </acceptance_criteria>
  <done>public/index.html has all R1 DOM markup removed; rail bottom is theme+settings (2 icons); modal block gone; version-remote row gone.</done>
</task>

<task type="auto">
  <name>Task 4.2: Delete clideck-remote driver and WS arms in app.js + state.js + settings.js</name>
  <files>public/js/app.js, public/js/state.js, public/js/settings.js</files>
  <read_first>
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-UI-SPEC.md ("Deletion contract" + "Removed copy")
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-CONTEXT.md (D-01, D-02, D-03)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-RESEARCH.md (§1c, §1d, §1e — line numbers verified at 9f6a111; the driver block ends at 1863 NOT 1816)
    - .planning/2026-06-02-mobile-desktop-concurrent-access/15-PATTERNS.md (§4 — app.js + §5 — settings.js + §6 — state.js)
    - public/js/app.js (read targeted ranges 90-220, 420-500, 1500-1880 — large file, do not read full)
    - public/js/state.js (read full file — 14 lines)
    - public/js/settings.js (read targeted range 95-120)
    - public/js/terminals.js (verify: this file is touched by Plan 05, NOT by this plan — do NOT modify here)
    - tests/sessions-resize.test.js (existing — should still pass after this plan)
    - e2e/clideck-remote-deletion.spec.js (Wave 0 — flips RED → GREEN after this plan)
    - e2e/smoke.spec.js (existing — must continue to pass after deletions, asserting `errors == []`)
  </read_first>
  <behavior>
    public/js/state.js:
    - `state.remoteVersion` field removed from the literal (line 13 per RESEARCH.md §1e / PATTERNS.md §6)
    - The `otherClientsConnected` field is added in Plan 05 — NOT here. Per `depends_on: [12-01]` Plan 05 runs in Wave 3 and depends on this plan's state.js cleanup to avoid a merge conflict.

    public/js/settings.js:
    - The two lines reading `version-remote` removed (lines 103-104 per RESEARCH.md §1d / PATTERNS.md §5)
    - `updateVersionFooter` continues to read `version-clideck` (line 100-102 preserved)
    - `window.__refreshStatusBadge` nudge stays

    public/js/app.js:
    - The lone `send({ type: 'remote.status' });` inside `state.ws.onopen` (line 107 per RESEARCH.md §1c) is deleted
    - The 7 `case 'remote.*':` arms in the `onmessage` switch (lines 437-461 per RESEARCH.md §1c) are deleted — `remote.status`, `remote.paired`, `remote.unpaired`, `remote.error`, `remote.install.progress`, `remote.install.done`, `remote.update`
    - The entire remote driver block (lines 1523-1863 per RESEARCH.md §1c) is deleted — last live line in the block is the `document.getElementById('remote-disconnect2').addEventListener…` line; next live code at line 1865 is `initDrag();` which STAYS
    - Module-level identifiers `remoteUpdateInfo`, `remotePreflight`, `remoteStatusPoll`, `remoteLastStatus`, `remoteState`, `remoteInstalled`, `remoteModalOpen`, `btnRemote`, `remoteModal` are all gone (they live inside the deleted block)
    - NO new `case 'clients.count':` arm is added in THIS plan — that lives in Plan 05 alongside the indicator wiring (since `updateOtherClientIndicator` is imported from terminals.js which Plan 05 modifies)
  </behavior>
  <action>
    Sub-task 4.2a — public/js/state.js (14-line file):

    Read the full file. Confirm the literal shape matches PATTERNS.md §6a. Delete the line `remoteVersion: null,` (line 13 per RESEARCH.md §1e). Do NOT add `otherClientsConnected: false,` in this task — that's Plan 05's territory. After deletion the literal should match PATTERNS.md §6a minus the `remoteVersion: null,` line. Verify with `node --check public/js/state.js` and `grep -c "remoteVersion" public/js/state.js` (expect 0).

    Sub-task 4.2b — public/js/settings.js:

    Read lines 95-120 (a focused range covering `updateVersionFooter`). Confirm the function shape matches PATTERNS.md §5a. Delete the two lines:
        `const remoteEl = document.getElementById('version-remote');`
        `if (remoteEl) remoteEl.textContent = state.remoteVersion || '';`
    (lines 103-104 per RESEARCH.md §1d). After deletion, the function shape matches PATTERNS.md §5b — `version-clideck` read stays, `__refreshStatusBadge` nudge stays. Verify with `grep -c "version-remote\\|remoteVersion" public/js/settings.js` (expect 0).

    Sub-task 4.2c — public/js/app.js (large file ~1900 lines):

    Read targeted ranges, NOT the full file. Use:
    - Read lines 90-115 to locate `send({ type: 'remote.status' });` inside `state.ws.onopen` at line ~107.
    - Read lines 430-465 to locate the 7 `case 'remote.*':` arms (per PATTERNS.md §4d).
    - Read lines 1515-1870 to locate the driver block boundaries (per PATTERNS.md §4e: block ends at 1863, `initDrag();` at 1865 stays).

    Edit A — Delete the single line `send({ type: 'remote.status' });` inside `state.ws.onopen` (line ~107 per RESEARCH.md §1c). The surrounding lines stay intact (the `onopen` handler keeps its other initial sends).

    Edit B — Delete the 7 `case 'remote.*':` arms (lines 437-461 per RESEARCH.md §1c). Per PATTERNS.md §4d the arms are: `case 'remote.status':`, `case 'remote.paired':`, `case 'remote.unpaired':`, `case 'remote.error':`, `case 'remote.install.progress':`, `case 'remote.install.done':`, `case 'remote.update':`. Delete from the first `case` keyword through the last arm's closing `break;`. Surrounding switch structure (e.g. `case 'closed':` before, `default:` after, `case 'sessions':` etc.) stays intact.

    Edit C — Delete the driver block (lines 1523-1863 per RESEARCH.md §1c). The block contains: modal control, install spinner, install-failed log, `clideck-remote` install path, `handleRemoteStatus / handleRemotePaired / handleRemoteUnpaired / handleRemoteError / appendInstallLog / handleInstallDone / finishRemotePreflight` definitions, the module-level identifiers, all event listeners on `#remote-*` elements. Verify the START boundary by finding the comment / function declaration that opens the block; verify the END boundary by confirming line 1864 (or wherever `initDrag();` is) is the next live line.

    Run `node --check public/js/app.js` to catch syntax errors. Open the file briefly with a final grep:
        `grep -nE "remote\\.(status|paired|unpaired|error|install|update)|remoteUpdateInfo|remotePreflight|remoteStatusPoll|remoteLastStatus|btnRemote|remoteModal|remoteState|remoteInstalled|remoteModalOpen|version-remote|remoteVersion|handleRemote|appendInstallLog|finishRemotePreflight" public/js/app.js`
    Expected: ZERO matches.

    Verify the full-repo R1 grep from D-03 / RESEARCH.md §1h is clean (excluding CHANGELOG and .planning):
        `git grep -nE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|btn-remote|version-remote|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate|remoteVersion|remoteUpdateInfo|remotePreflight|remoteStatusPoll|remoteState|remoteInstalled|remoteModalOpen|remoteLastStatus|btnRemote|remoteModal" -- ':!CHANGELOG.md' ':!.planning/' ':!docker-compose*.yml' ':!Dockerfile*'`

    Expected: zero matches. If matches remain, locate and clean them before considering this task done.

    Run `npm run test` to ensure no unit regressions. tests/sessions-resize.test.js stays GREEN (Plan 02), tests/other-client-indicator.test.js stays RED (Plan 05 not yet landed). Other tests should pass.

    NOTE: This task DOES NOT add the `case 'clients.count':` arm to app.js — that arm imports `updateOtherClientIndicator` from `./terminals.js`, which is created in Plan 05. Adding it now would break the build.
  </action>
  <verify>
    <automated>set -e; node --check public/js/state.js && node --check public/js/settings.js && node --check public/js/app.js && echo "All 3 files parse"; REPO_GREP=$(git grep -nE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|btn-remote|version-remote|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate|remoteVersion|remoteUpdateInfo|remotePreflight|remoteStatusPoll|remoteState|remoteInstalled|remoteModalOpen|remoteLastStatus|btnRemote|remoteModal" -- ':!CHANGELOG.md' ':!.planning/' ':!docker-compose*.yml' ':!Dockerfile*' 2>/dev/null | wc -l); echo "Repo R1 grep matches: $REPO_GREP (expect 0)"; test "$REPO_GREP" = "0"; npm run test 2>&1 | tee /tmp/12-04-vitest.log</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/js/state.js` exits 0
    - `node --check public/js/settings.js` exits 0
    - `node --check public/js/app.js` exits 0
    - `grep -c "remoteVersion" public/js/state.js` returns 0
    - `grep -c "remoteVersion\\|version-remote" public/js/settings.js` returns 0
    - `grep -c "case 'remote\\." public/js/app.js` returns 0 (all 7 arms deleted)
    - `grep -c "remote.status\\|remote.paired\\|remote.unpaired\\|remote.error\\|remote.install\\|remote.update" public/js/app.js` returns 0
    - `grep -c "remoteModal\\|btnRemote\\|remoteUpdateInfo\\|remotePreflight\\|remoteStatusPoll\\|remoteLastStatus\\|remoteState\\|remoteInstalled\\|remoteModalOpen\\|handleRemote\\|appendInstallLog\\|finishRemotePreflight" public/js/app.js` returns 0
    - `grep -c "initDrag" public/js/app.js` returns ≥ 1 (the live line after the deleted block survives)
    - Full repo R1 grep (D-03 + RESEARCH.md §1h pattern) returns 0 matches outside CHANGELOG / .planning / docker-compose / Dockerfile
    - `npm run test` exits 0 (unit suite passes — sessions-resize green, other-client-indicator still red as expected)
    - State.js literal does NOT yet contain `otherClientsConnected` (Plan 05 adds it; this task only removes `remoteVersion`)
    - Verbose commit message references R1 + D-01/D-02/D-03 + the 4 files touched per CLAUDE.md §5
  </acceptance_criteria>
  <done>All 4 client-side files (index.html, app.js, settings.js, state.js) have R1 remote references removed. Full-repo R1 grep is clean. Unit suite green. Plan 05 will add the new `case 'clients.count':` arm + indicator markup + state flag.</done>
</task>

</tasks>

<verification>
- `git grep -nE "remote-modal|clideck-remote|remote\\.(update|error|installing|status|pair|unpair|history|paired|unpaired|install\\.progress|install\\.done|getHistory)|btn-remote|version-remote|remoteCliEnv|remoteUpdateCache|REMOTE_UPDATE_INTERVAL|checkRemoteUpdate|remoteVersion|remoteUpdateInfo|remotePreflight|remoteStatusPoll|remoteState|remoteInstalled|remoteModalOpen|remoteLastStatus|btnRemote|remoteModal" -- ':!CHANGELOG.md' ':!.planning/' ':!docker-compose*.yml' ':!Dockerfile*'` → zero matches
- All 4 modified files parse with `node --check`
- `npm run test` exit 0 — unit suite green (sessions-resize green, other-client-indicator RED is expected)
- If Chromium libs available, `npx playwright test e2e/smoke.spec.js e2e/clideck-remote-deletion.spec.js` → both green (smoke asserts no console errors, deletion spec asserts toHaveCount(0) on the three IDs). If not available, document in the SUMMARY.
</verification>

<success_criteria>
Plan 04 is complete when:
- [ ] public/index.html: btn-remote, remote-modal, version-remote DOM all gone
- [ ] public/js/app.js: send remote.status removed; 7 case arms removed; 1523-1863 driver block removed; initDrag() at line 1865 still live
- [ ] public/js/settings.js: version-remote read removed; version-clideck stays
- [ ] public/js/state.js: remoteVersion field removed; otherClientsConnected NOT yet added (Plan 05's territory)
- [ ] Full-repo R1 grep returns zero matches (per D-03 / RESEARCH §1h)
- [ ] Unit suite passes (`npm run test` exit 0)
- [ ] Commit message verbose per CLAUDE.md §5, references R1 + D-01..D-03
</success_criteria>

<output>
Create `.planning/2026-06-02-mobile-desktop-concurrent-access/15-04-SUMMARY.md` when done — list the 4 files modified, the line ranges deleted from each, confirm the full-repo R1 grep is clean, and confirm the unit suite is green. Note that R5 client wiring (the `case 'clients.count':` arm + indicator markup + state flag + tailwind rebuild) is Plan 05.
</output>
